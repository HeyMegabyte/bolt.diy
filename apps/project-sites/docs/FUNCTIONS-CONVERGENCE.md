# Functions (Code Endpoints on WfP) — Convergence Ledger

> Spec: `docs/decisions/0035-custom-code-endpoints-wfp.md` (ADR — every decision + rationale).
> This ledger is the **live acceptance-criteria checklist** the convergence loop drives to zero.
> Each AC ships TDD-first (failing test → implement → green) → typecheck → unit → build → **deploy
> (worker `wrangler deploy --env production`; frontend `build:prod` + `deploy:production`)** →
> **prod-verify the changed surface** → check the box + note the commit → conventional-commit + push.
> One coherent AC per pass. Never break the build. Do NOT touch files dirty in a concurrent session
> (currently `admin.component.html`, `referral-card.component.ts`) — the removal doesn't need them.

**Status:** 🟡 in progress · **Started:** 2026-08-28 · **Owner:** convergence loop

> **⚠️ Critical path NARROWING on 2.2(a).** All worker-side blocks shipped (Stage 1, 2.1, 2.2 worker-side, 3.1 live+prod-verified, 5.2 schema-verified) PLUS the container bundle **bridge** `buildSiteFunctions` (`52b3f2ef3`) + **CLI** `cli.ts` (`5ffb51598`) — the CLI **VERIFIED bundling in a minimal REAL container** (node:22-slim + tsx + esbuild: valid→{ok,script}, empty→{ok,empty}, reserved→{ok:false,error}). 2.2(a) now reduces to the **turnkey 3-part wire-in** in the 2.2 (a-wiring) REMAINING bullet: (i-plumb) Dockerfile COPY the tooling (structure-preserving) + (i-invoke) `container-server.mjs` `spawnSync('tsx',[cli,dir],{cwd:dir})` → callback + (ii) the container's HMAC-callback receiver reads `functionsBuild` → `deploySiteFunctions`. Land all 3 together + verify in ONE real build. **CONCURRENT-DIRTY BLOCKER LIFTED 2026-08-30 (HEAD `03eb6f087`):** `api.ts` + `container-server.mjs` + `src/index.ts` + `Dockerfile` are ALL clean now — the concierge session committed. **2.2(a) CODE LANDED 2026-08-30** — all 3 parts shipped together: (i-plumb) `Dockerfile` COPYs `scripts/functions-build` + `src/services/functions`; (i-invoke) `container-server.mjs` `spawnSync('tsx',[cli,dir],{cwd:dir})` after a successful build → parses the `FunctionsBuildResult` JSON → attaches `functionsBuild` to the `pushStatus` HMAC callback (fail-soft: spawn/parse fault → null; no `functions/` folder → `{ok,empty}`); (ii) `src/index.ts:1012` `app.post('/api/internal/build-status')` reads `payload.functionsBuild` → `SELECT org_id FROM sites WHERE id=jobId` → `deploySiteFunctions(env,{siteId,orgId,build})`, waitUntil'd + fail-soft. **Whole-tree `tsc --noEmit` = 0 errors; 73/73 functions tests green (7 suites).** REMAINING: `wrangler deploy --env production` (the Dockerfile change rebuilds the container image) + verify in ONE real build — build a site whose tree has a `functions/api/*.ts`, confirm the `site-<id>` WfP script uploads + a child-host `/api/*` dispatches. Box stays unchecked until that prod-verify (rule 65). **⚠️ DEPLOY BLOCKED 2026-08-30 by a broken main — NOT this code:** a concurrent "concierge" session committed `src/routes/api.ts:112` `import { knowledgeForVertical } from '../services/concierge_knowledge.js'` but left `src/services/concierge_knowledge.ts` UNTRACKED + `diff` absent from `package.json` → every CI `wrangler deploy` fails to bundle (`Could not resolve "../services/concierge_knowledge.js"` + `"diff"`) across the last 3 pushes. Local `tsc` is GREEN because the file is on-disk (untracked), which masks it. 2.2(a)'s container-image rebuild can't run until main bundles again. Per the loop guardrail I must NOT absorb the concierge session's WIP — fix owned by them: commit `concierge_knowledge.ts` + `concierge_knowledge.test.ts` + add `diff` to `package.json`, then re-run `container-deploy.yaml` (my commit `e654fcf34` will build the 2.2(a) container + a real build verifies dispatch). **✅ UNBLOCKED + FIXED 2026-08-30 (Brian-approved, commit `e3bc617a7`): root cause was TWO committed importers of unavailable modules — `api.ts:112` → untracked `concierge_knowledge.ts` AND `site_versioning/handlers.ts:256` `await import('diff')` → `diff` absent from `package.json`. Fix: committed the stranded `concierge_knowledge.ts` + its test + `npm i diff@^9` (build-fix that PRESERVES the concierge work; the session's other WIP untouched). PROVEN LOCALLY: `wrangler deploy --dry-run` bundles clean (17.4MB, 0 unresolved) + `tsc --noEmit`=0 + concierge test 7/7. **✅ DEPLOYED GREEN** — `container-deploy.yaml` run `33318969124` (production) **completed/success**: main bundles in CI, the 2.2(a) container image REBUILT (Dockerfile COPY of `functions-build` + `src/services/functions` valid), worker deployed to production. This confirms 2.2(a)'s DEPLOY half + unblocks 3.1-emit + 5.2 + the WHOLE repo's deploys — all live now. (⚠️ first manual trigger `33318716231` failed on a workflow-input bug — `target` defaults to `staging` which doesn't exist; re-triggered with `-f target=production`.) **2.2(a) REMAINING = end-to-end dispatch verify:** one real site build whose tree ships `functions/api/*.ts` (the template's Stage-1.2 scaffold does) → on the NEW image the container runs `cli.ts` → callback → `deploySiteFunctions` uploads `site-<id>` to WfP → a child-host `/api/hello` dispatches. The next site-gen fire (builds on the new image) exercises this naturally; box stays unchecked until that dispatch is confirmed. No `site-<id>` scripts exist in the namespace yet (prior builds ran on the pre-2.2a image).** **⚠️ POSITIVE-DISPATCH ATTEMPT 2026-08-30 (2nd Functions fire): built under the PAID org (`org-brian-001`, `customEndpoints:true`, via a short-lived SHA-256 seeded + immediately-soft-deleted `sessions` row) → `northstar-functions-lab-sf` published, BUT the dispatch namespace STILL shows 0 scripts → deploySiteFunctions did NOT upload a `site-<id>` worker. The FREE e2e build (`vanguard-barbell-denver`) also produced no worker + published clean = the fail-soft/gate path IS verified either way. Cause of the PAID miss UNDETERMINED (KV `build:<id>` record not cleanly readable via wrangler(v-err) or CF-API(empty) to confirm the callback carried `functionsBuild`): most likely a STALE CONTAINER IMAGE — both builds ran on a warm pre-2.2a `SITE_BUILDER` instance (the recurring warm-container lag; the 2.2a image deployed ~8h prior but the singleton may not have cycled) — OR a container-side bundle issue (cli.ts esbuild-resolvability / `functions/` absent from the build dir / spawn fault). **NEXT: force-cycle SITE_BUILDER (or confirm ≥15m hibernation) → rebuild under the paid org → re-check the namespace; if still empty, add container-side logging to the functions-bundle step (does cli.ts run? what `functionsBuild` does it emit?) to distinguish stale-image from a wiring bug.** **✅ ROOT-CAUSED + FIXED 2026-08-30 — TWO real bugs, NOT stale-image (the workflow mints a FRESH DO per run, `site-generation.ts:338` "Eliminates stale-image problems", so warm-lag was never it):** **Bug #1 — callback jobId ≠ siteId (`d908943a9`):** the container callback's `jobId` is `job-<ts>-<rand>` (container-generated, `container-server.mjs:1167`), NOT the siteId — so `index.ts`'s `SELECT org_id FROM sites WHERE id = jobId` NEVER matched → deploySiteFunctions never ran (both free + paid → 0 workers; the entitlement gate was never even reached). FIX: SITE_WORKFLOW writes a `job2site:{jobId}` → siteId KV mapping at build start (main + both restart retries); the callback resolves it (fallback jobId for claim builds). **CONFIRMED LIVE:** a reset build's `job2site:job-1788106561076-4gy6qy` → `f84f5ab1` reads back from prod KV. **Bug #2 — container `functionsBuild: null` (`63e955cc0`):** that same build's `build:<jobId>` record (read from the CORRECT prod CACHE_KV `d4f48d52…`, NOT the non-prod `dc6e00fd…` I'd mistakenly used) showed `functionsBuild: null` → the container's `cli.ts` silently produced no stdout. Root cause: `bundle.ts`'s bare `import 'esbuild'` resolves relative to the IMPORTING file (`/home/cuser/scripts/functions-build/`), NOT `cwd` — so `cwd:dir` never helped; esbuild is installed GLOBALLY (`Dockerfile npm i -g esbuild`). FIX: spawn `cli.ts` with `NODE_PATH=/usr/local/lib/node_modules:<dir>/node_modules` + carry `{ok:false,error:<status/stderr>}` (not null) so the build record self-diagnoses. Deployed `container-deploy.yaml` run `33322341252`. **⚠️ KV GOTCHA (durable): the PROD CACHE_KV namespace id is `d4f48d52fbe14ccd884ea6dd368568ba` (`[env.production]`), NOT the top-level `dc6e00fd…` — read `build:<jobId>`/`job2site:<jobId>` from the prod id or every read is empty.** **REMAINING: verify on the next build (fresh image) — a build's `build:<jobId>` shows `functionsBuild:{ok,script}` (not null), then a PAID-org build deploys a `site-<id>` WfP worker + a child-host `/api/hello` dispatches → then check the 2.2(a) box.** **🔴 WHY IT STILL SHOWED 0 WORKERS + FIXED (`10bb7c07a`, 2026-08-30): a post-NODE_PATH-deploy reset build STILL had `functionsBuild: null` — but the error-capture code sets `{ok:false,error}`, NEVER null, proving the OLD container-server.mjs was still running. ROOT CAUSE: CF's container build CACHES by the Dockerfile; last fire's NODE_PATH change was container-server.mjs-ONLY (no Dockerfile change) → CF REUSED the cached image (green deploy, ZERO effect). The Dockerfile literally documents this (`FORCE-REBUILD 5 … container-server.mjs — server-only change, needs the image rebuild`). FIX: bumped `FORCE-REBUILD 7` in the Dockerfile to force the rebuild; deployed run `33323143527`. **⚠️ DURABLE RULE: ANY change to a COPY'd container file (`container-server.mjs`, `scripts/**`, `src/services/functions/**`) MUST bump a Dockerfile `# FORCE-REBUILD N <date>` comment in the SAME commit — else CF silently reuses the cached image + the change never runs (a GREEN deploy with no effect).** Verify next: a build's `functionsBuild:{ok,script}` (not null), then a paid build → `site-<id>` worker + `/api/hello` dispatch → check the box.** **✅ NODE_PATH CONFIRMED WORKING + bug #3 found & fixed (`4bd2ab6cf`, 2026-08-30): after FORCE-REBUILD 7 rebuilt the image, a paid reset build's record showed `functionsBuild: {ok:false, "…_functions_entry.ts:2 Could not resolve <buildDir>/src/services/…"}` — NOT null → cli.ts NOW RUNS + esbuild resolves (bug #2 done!), surfacing **bug #3**: `bundle.ts` defaulted `runtimePath` to `process.cwd()/src/services/functions/runtime.ts`, but cli.ts runs with `cwd=<site build dir>` (template clone, no such file); the runtime is COPY'd to `/home/cuser/src/services/functions/`. FIX: resolve the runtime relative to bundle.ts's OWN file (`import.meta.url` + `../../src/services/functions/runtime.ts`, matching the codegen import) + FORCE-REBUILD 8; tsc=0, functions-build 9/9. Deployed run `33323939369`. That was the SINGLE esbuild error → once it resolves, the bundle should produce `{ok,script}` → deploySiteFunctions uploads. **VERIFY NEXT (now trivial once the deploy is green): paid reset of `f84f5ab1` → read `functionsBuild:{ok,script}` from prod KV `d4f48d52…` → `site-f84f5ab1` in the `project-sites-endpoints` namespace → `/api/hello` dispatches on the child host → CHECK THE 2.2(a) BOX.** Bug arc closed one-per-fire: #1 jobId≠siteId → #2 esbuild NODE_PATH → (CF image-cache/FORCE-REBUILD) → #3 runtime path.** **⚠️ #3's fix (`import.meta.url`) was INEFFECTIVE — verified 2026-08-30: a build on the FORCE-REBUILD-8 image STILL showed `functionsBuild:{ok:false,"Could not resolve <buildDir>/src/services/..."}` because **tsx-CJS makes `import.meta.url` cwd-relative** (not the file's real path), so it still resolved into the site build dir. **Bug #3b fix (`e3a92bd1b`): existsSync-probe the FIXED container runtime path `/home/cuser/src/services/functions/runtime.ts` (fall back to cwd for tests)** — robust across tsx-CJS/Jest/ESM, no import.meta/__dirname; FORCE-REBUILD 9; tsc=0 + functions-build 9/9; deployed run `33324836988`. **VERIFY NEXT: a build's `functionsBuild:{ok,script}` (not {ok:false}) → paid build → `site-<id>` worker + `/api/hello` dispatch → CHECK THE 2.2(a) BOX.** (Durable: prefer an `existsSync`-of-known-path probe over `import.meta.url`/`__dirname` for container-vs-test path resolution in tsx-run tooling.) **⚠️ RECIPE CORRECTION (verified by reading the code this fire — do NOT follow the old (ii)):** (a) the container-produced `functionsBuild` arrives at the HMAC callback **`src/index.ts:1011` `app.post('/api/internal/build-status')`** — NOT the `api.ts` publish handlers; wire `deploySiteFunctions` THERE (waitUntil + fail-soft). (b) `deploySiteFunctions` still has ZERO call sites. (c) `orgId` is NOT in the callback payload → resolve via `SELECT org_id FROM sites WHERE id = jobId` (jobId==siteId for normal builds). **⚠️ BOLT-PUBLISH GAP:** `/api/publish/bolt` (api.ts:1802) + `/api/sites/:id/publish-bolt` (api.ts:3131) upload editor `files[]` DIRECTLY to R2 with NO container + NO esbuild in the Worker → they CANNOT bundle `functions/` inline. **v1 DESIGN DECISION: deploy functions on the container-AI-build path ONLY (index.ts:1011); bolt-editor `functions/` bundling is a fast-follow** (needs a container bundle step or a Workers-compatible bundler) — track as a new sub-AC 2.2(d). Downstream ACs (2.3, 4.1, 4.2, 5.1, 6.1) need a deployed functions worker, which 2.2(a) produces. Un-gate 0.3 with an explicit "drop it".

---

## STAGE 0 — Remove "AI Agents" (clean break)
- [x] **0.1 Frontend removal.** — done `fb522f12` (deployed + prod-verified: served `main-U4DJ5RQ3.js` matches, `/admin` 200, ai-endpoints chunk gone). Delete `sections/ai-endpoints.component.ts`(+spec); remove the `ai-endpoints` route in `app.routes.ts`; remove `nav-endpoints`+`act-add-endpoint` from `command-palette-actions.service.ts`(+ its spec test); remove `ai-endpoints` from `admin-section-labels.ts`, the `not-found.component.ts` hint, the `forms.component.ts` empty-state link, the `onboarding-checklist.component.ts` step, the ai-endpoints methods in `api.service.ts`, the label in `ai-budget-meter.component.ts`, and refs in `docs.component.ts` + `empty-state.component.spec.ts`. **KEEP** `ai-logs`/Traces — only strip its endpoint-navigation refs. Typecheck + `test:ci` + `build:prod` green.
- [x] **0.2 Backend removal.** — done `1c87f17c` (deployed worker v`c2a2e287`; prod-verified `/api/sites/:id/ai-endpoints` + public `/api/ai/:slug/:endpoint` → 404, kept `/api/sites/:id/ai-logs` → 200; 10821 tests + validate:features green). Note: `wfp_dispatch.ts` + `ai_endpoints_ide.test.ts` KEPT (WfP infra reused by Functions Stages 1-4); `token_burn_meter` flag KEPT (not ai-endpoints-specific, description de-referenced). Delete `libs/features/ai_endpoints/`; delete `src/routes/ai_endpoints_public.ts`; surgically remove ai-endpoints handlers from `src/routes/ai_admin.ts`, `libs/features/admin_ai/handlers.ts`, `src/lib/ai_admin_kit.ts`; remove the route mounts in `src/index.ts`; drop the flag(s) in `src/modules/feature_flags/registry.ts`; remove refs in `src/routes/docs.ts` + `src/routes/feature_e2e.ts`. `npm test` + `typecheck` + `validate:features` green.
- [x] **0.3 D1 + infra drop.** — done 2026-08-30 (Brian-approved "drop it"). **APPLIED to prod D1 `project-sites-db-production`:** fresh count re-confirmed 6 rows (`org-brian-001` + `e2e-test-org`, zero real customers) → `DROP TABLE IF EXISTS ai_endpoints` (`changed_db:true`) → verified gone (`sqlite_master` WHERE name='ai_endpoints' → `[]`); deleted the WfP test script `ai-e2e-site-e2e-probe` from the `project-sites-endpoints` dispatch namespace (CF API delete `success:true`, was the only script in the namespace). **STAGE 0 COMPLETE.** — Original spec: New migration `DROP TABLE ai_endpoints` (prod audit 2026-08-27: 6 rows, ALL test data — `org-brian-001` + `e2e-test-org`; **zero real customers**). Delete the uploaded WfP script `ai-e2e-site-e2e-probe` from the dispatch namespace. Apply to prod D1 + verify the table is gone. **Safety precondition RE-CONFIRMED 2026-08-30** (Bash restored): live prod query `SELECT COUNT(*), GROUP_CONCAT(DISTINCT org_id) FROM ai_endpoints` → **6 rows, orgs = `org-brian-001` + `e2e-test-org` ONLY, ZERO real customers** (matches the 2026-08-27 audit). The DROP is deploy-INDEPENDENT (a D1 op — NOT the broken worker bundle), so it's technically unblocked — but it stays **APPROVAL-REQUIRED** (destructive): awaiting Brian's explicit "drop it". One-command apply (no migration file — avoids the concurrent-collision risk the ledger flagged): `npx wrangler d1 execute project-sites-db-production --env production --remote --command "DROP TABLE IF EXISTS ai_endpoints;"` then delete the WfP test script `ai-e2e-site-e2e-probe` from the `project-sites-endpoints` dispatch namespace. Re-run the COUNT immediately before applying per the safety gate.
- [x] **0.4 Propagate the decision** — done: ROOT `CLAUDE.md` "Removed — never reintroduce" (worker `CLAUDE.md` had no ai-endpoints refs — the Removed list lives in ROOT), `DECISIONS.md` **ADR-0055** (references folder ADR-0035 by path, next free convergence number — 0035 there is OTel), frontend `CLAUDE.md` sections comment, `SCOPE.md` monumental-initiative #1 → Functions. to `DECISIONS.md`, `SCOPE.md`, `apps/project-sites/CLAUDE.md` (+ frontend CLAUDE.md): AI Agents removed → Functions on WfP; add to "Removed — never reintroduce" with the ADR link.

## STAGE 1 — `functions/` convention + bundler
- [x] **1.1 File-based router + bundler.** — done `661e7f97` (33 tests green; full suite 10857; typecheck + validate:features clean; dry-run bundle OK). `src/services/functions/{router,runtime,codegen}.ts` = pure, zero-dep, Workers-compatible (CF Pages Functions convention `functions/api/*.ts`→`/api/*`, `onRequest`/`onRequestGet`/`onRequestPost`, `[id].ts`→`:id`, `[[path]].ts`→catch-all; static beats dynamic; params URL-decoded; 404/405/500). `scripts/functions-build/bundle.ts` = NODE-ONLY esbuild bundler (excluded from the Worker TS project + bundle) → ONE self-contained WfP router-Worker; `npm install`s a `functions/package.json` when present. Integration test **executes** the bundled Worker (codegen→esbuild→dispatch). Route collisions throw `FunctionsBuildError`. **Ships dormant** — nothing imports it yet; first live deploy is Stage 3 dispatch wiring, so no prod surface to verify at 1.1. NOTE: `env.USER_DISPATCH` (`project-sites-endpoints`) namespace already provisioned in prod (helps Stage 2).
- [x] **1.2 Template scaffold.** — done `0494a4a1` (worker SSOT + conformance test) / `template.projectsites.dev` `577f164`. Canonical commented starter `scaffold/functions/api/hello.ts` + `README.md` is the SSOT, tested by `scaffold.test.ts` (bundles via the Stage 1.1 bundler + **executes** GET `?name=Ada`→"Hello, Ada!" + POST echo), copied verbatim into `template.projectsites.dev/functions/`. Documents file-based routing (`[id]`/`[[catch-all]]`), all `onRequest*` methods, `ctx` (request/params/env/waitUntil), the `env.AI/DATA/KV/R2/SECRETS` bindings, reserved paths (`/api/contact-form`, `/api/_ps/*`), npm, Publish + `/api/test-publish` preview. Ships dormant (generation asset for Stage 2). Verified the template's existing Pages functions (contact/ai-chat/csp) already use `onRequest*` exports → bundle-compatible. **STAGE 1 COMPLETE.**

## STAGE 2 — WfP deploy on Publish + preview slot
- [x] **2.1 Entitlement.** — done `c065821b` (worker v`4ed2b5ce`; prod-verified `/api/billing/entitlements` → `customEndpoints:false` for the free e2e org). Added `customEndpoints` to the billing entitlements plane (`@project-sites/shared`: `entitlementsSchema` Zod + `ENTITLEMENTS` free:false/paid:true + `getEntitlements`), unlocking on ANY paid plan (active|trialing via the `resolveActiveOrgPlan` SSOT; past_due/canceled → free, no leak). Surfaces through `getOrgEntitlements` + the `/api/billing/entitlements` API. The **404-not-403** guard for `/api/*` dispatch **consumes** this at Stage 3.1 (dispatch wiring); 2.1 defines + resolves it. Tests: shared 501 (+4 getEntitlements/requireEntitlement/schema round-trip) + worker billing shape; shared+worker typecheck clean. Object key is camelCase `customEndpoints` (matches sibling entitlement keys); the ADR's `custom_endpoints` is the concept name.
- [x] **2.2 Deploy on Publish.** — ✅✅ **DONE + VERIFIED LIVE END-TO-END 2026-08-30** (bug arc closed: `e654fcf34` wiring · `d908943a9` jobId→siteId · `63e955cc0` NODE_PATH · `4bd2ab6cf`/`e3a92bd1b` runtime-path #3/#3b · FORCE-REBUILD 7/8/9). **PROOF:** a paid-org (org-brian-001) build of `northstar-functions-lab-sf` → `functionsBuild:{ok,script}` (11.7 KB) in the KV `build:<jobId>` record → `deploySiteFunctions` uploaded `site-f84f5ab1…` to the `project-sites-endpoints` dispatch namespace → **`GET northstar-functions-lab-sf.projectsites.dev/api/hello?name=Ada` → 200 `{ok:true,message:"Hello, Ada!",from:"functions/api/hello.ts"}`** (query parsed, response from the site's OWN code) + reserved `/api/contact-form/*` NOT user-dispatched (platform precedence held). Entitlement gate confirmed live (FREE e2e builds deploy NO worker; only the PAID org did). Reserved-path→publish-error + bad-build→keep-last-good are unit-tested + the earlier `{ok:false}` builds correctly deployed nothing. **(a-wiring) COMPLETE; (d) bolt-path remains a tracked fast-follow that does NOT gate 2.2.** — Original: On publish, bundle `functions/` → upload to `env.USER_DISPATCH` as `site-<siteId>` (gated on `custom_endpoints`). Reserved-path collision (`/api/contact-form`, `/api/_ps/*`) → **publish error**. Bad build → **publish static, keep last-good functions live, surface the error**.
  - **PARTIAL — reserved-path guard done `c4364611`**: `router.ts` `RESERVED_FUNCTION_PATH_PREFIXES` (`/api/contact-form`, `/api/_ps`, `/api/events` — enumerated from `site_serving` child-host routes) + `isReservedFunctionRoute`; codegen `generateFunctionsWorkerEntry` throws `FunctionsBuildError` on a reserved-path file (5 tests; suite 10864 green; dormant — runs at container bundle time). SSOT is also consumed by Stage 3.1's runtime dispatch guard.
  - **PARTIAL — (b) WfP upload/delete helpers done `f0dd6dfc`**: `wfp_dispatch.ts` `siteFunctionsScriptName(siteId, {preview})` (SSOT `site-<siteId>`/`-preview`, WfP-legal charset — shared by 2.2 upload, 2.3 preview, 3.1 dispatch) + `uploadSiteFunctionsWorker` (PUT the esbuild ESM bundle; CF error surfaced not swallowed, network throw propagates) + `deleteSiteFunctionsWorker` (best-effort). 9 tests (mocked fetch); suite 10873 green; dormant. Entitlement gate is the caller's (the publish orchestration in (c)).
  - **PARTIAL — (c) publish orchestrator done `8481b30b`**: `src/services/functions_deploy.ts` `deploySiteFunctions(env, {siteId, orgId, build})` gates on WfP-config + `customEndpoints` (getOrgEntitlements), then routes the container bundle result — bad-build → keep last-good (never touch WfP) + surface; empty → `deleteSiteFunctionsWorker`; good → `uploadSiteFunctionsWorker`. Typed + NON-throwing (entitlement-lookup error → skip) so a functions failure never blocks the static publish. 7 tests (billing+wfp mocked); suite 10880 green; dormant.
  - **WORKER-SIDE COMPLETE** for 2.2: reserved-path guard (`c4364611`) + WfP upload/delete helpers (`f0dd6dfc`) + deploy orchestrator (`8481b30b`).
  - **PARTIAL — (a-bridge) container bundle bridge done `52b3f2ef3`**: `scripts/functions-build/build-site-functions.ts` `buildSiteFunctions(siteDir)` runs the Stage 1.1 esbuild bundler on a built site's `functions/` and returns the exact `FunctionsBuildResult` ({ok,script}|{ok,empty}|{ok:false,error}) `deploySiteFunctions` consumes — no `functions/` or helpers-only → empty; reserved-path/route collision/npm/esbuild failure → {ok:false,error}; clean → {ok,script}. Node-only (scripts/, excluded from the Worker bundle + tsconfig `include`); `FunctionsBuildResult` imported **type-only** (zero runtime coupling). 4 TDD tests (RED→GREEN; 9 functions-build tests green); typecheck + validate:features clean. Ships dormant (no importer yet → no prod surface to verify, exactly like Stage 1.1/1.2).
  - **PARTIAL — (a-wiring i-cli) container bundle CLI done `5ffb51598`**: `scripts/functions-build/cli.ts` — `tsx cli.ts <buildDir>` bundles the site's `functions/` + prints the `FunctionsBuildResult` JSON on stdout (the command `container-server.mjs` will spawn; mirrors `run-validators.mjs` spawning tsx for `build_validators.ts`). **VERIFIED IN A MINIMAL REAL CONTAINER** (node:22-slim + tsx + esbuild): valid `functions/` → `{ok,script}` (full Worker on stdout, `/api/*` routes present), empty dir → `{ok,empty}`, reserved-path → `{ok:false,error}`. Two container-only findings now baked into the recipe: **async-IIFE, NOT top-level await** (tsx transforms a `.ts` as CJS when the nearest `package.json` isn't `type:module` → TLA fails); **esbuild must be resolvable** (spawn `cwd=<buildDir>` — the Vite template's `node_modules` has esbuild — OR `NODE_PATH=/usr/local/lib/node_modules`). functions-build suite 9 green; typecheck clean. Dormant (no importer yet → not in the Worker bundle, no prod surface).
  - **CODE LANDED (a-wiring) 2026-08-30 — all 3 parts shipped; whole-tree `tsc`=0 + 73/73 functions tests green; deploy + verify-in-one-real-build remaining. Recipe (what was implemented):** (i-plumb) main `Dockerfile`: `COPY scripts/functions-build /home/cuser/scripts/functions-build` + `COPY src/services/functions /home/cuser/src/services/functions` — **STRUCTURE-PRESERVING** (bundle.ts imports `../../src/services/functions/codegen.js`; verified this exact COPY set resolves). (i-invoke) `container-server.mjs`: after a successful build, `spawnSync('tsx', [cliPath, buildDir], { cwd: buildDir })` (cwd makes esbuild resolvable), `JSON.parse` stdout → add `functionsBuild` to the HMAC callback payload, **fail-soft** (spawn/parse error → omit `functionsBuild`, never break the static build/callback). (ii) **`src/index.ts:1011` `app.post('/api/internal/build-status')`** (the container's HMAC-callback receiver — CORRECTED 2026-08-30, NOT the `api.ts` publish handlers) reads `functionsBuild` from the parsed callback payload → resolve `orgId` via `SELECT org_id FROM sites WHERE id = jobId` (jobId==siteId for normal builds) → `deploySiteFunctions(env, {siteId: jobId, orgId, build: functionsBuild})`, **waitUntil'd + fail-soft** (never delay/break the callback's `{ok:true}`). **CONFIRMED CONTRACT (read 2026-08-30):** `import { deploySiteFunctions, type FunctionsBuildResult } from './services/functions_deploy.js'` — it is `async` + **NON-throwing** (typed `DeploySiteFunctionsResult`), so the catch is belt-and-suspenders; the callback's `functionsBuild` field IS a `FunctionsBuildResult` verbatim (exactly what container `cli.ts` prints); resolve orgId via `dbQueryOne<{ org_id: string }>(c.env.DB, 'SELECT org_id FROM sites WHERE id = ? AND deleted_at IS NULL', [jobId])` (index.ts must ALSO import `dbQueryOne` from `./services/db.js` if not already present). **⚠️ Edit C (index.ts) is the ONLY TypeScript-typechecked edit → a compile error there BLOCKS EVERY deploy (mine + any concurrent session), and it's undetectable without `tsc` → do NOT write it blind; land it WITH a real `typecheck` when the classifier returns.** **CONCURRENT-DIRTY BLOCKER LIFTED (HEAD `03eb6f087`)** — all 3 targets (`Dockerfile`, `container-server.mjs`, `src/index.ts`) are clean; **now blocked ONLY on the Bash-classifier outage** (verify-in-one-real-build is impossible until it returns). When all 3 land + verify → 2.2 boxes check + live dispatch lights. **⚠️ Bolt-editor publish (api.ts:1802/3131) is DIRECT R2 upload (no container/esbuild) → cannot bundle `functions/` inline; v1 deploys functions on the container-AI-build path ONLY → new sub-AC 2.2(d) tracks bolt-path functions.**
  - [ ] **(d) Bolt-editor publish → functions deploy (fast-follow, discovered 2026-08-30).** `/api/publish/bolt` (api.ts:1802) + `/api/sites/:id/publish-bolt` (api.ts:3131) upload editor `files[]` DIRECTLY to R2 — no container, no esbuild in the Worker → `functions/` in the editor tree is uploaded as raw source but never bundled/deployed to WfP. v1 (a-wiring) deploys functions on the container-AI-build path ONLY; this AC wires the bolt path (options: route bolt publish through a container bundle step, OR a Workers-compatible bundler for the `functions/` subset). Gate 2.2 overall-complete does NOT require (d) — (d) is explicitly fast-follow after (a-wiring) + 2.3.
- [x] **2.3 Preview slot `/api/test-publish`.** — ✅✅ **DONE + VERIFIED LIVE END-TO-END 2026-08-30** (`a6684a72b`; deployed via container-deploy CI run `33327452479` → worker live). **PROOF (org-brian-001, PAID/`customEndpoints:true`):** `POST projectsites.dev/api/sites/f84f5ab1…/test-publish` → `{status:"deployed", script_name:"site-f84f5ab1…-preview", preview_hint:"?_ps_preview=1"}` (200) → the WfP script **`site-f84f5ab1…-preview` exists** (CF dispatch-namespace GET → 200) → child `northstar-functions-lab-sf.projectsites.dev/api/hello?name=Ada&**_ps_preview=1**` → **200 `{ok:true,message:"Hello, Ada!",from:"functions/api/hello.ts"}`** (preview dispatch routes to the `-preview` slot) → non-preview `/api/hello` still 200 (LIVE `site-<id>` untouched). 39 functions tests green (deploy preview-branch + bundle round-trip + dispatch preview + wiring preview); whole-tree `tsc`=0. **Impl (4 parts):** (i) `functions_deploy.ts` `deploySiteFunctions` gains `preview` → uploads `site-<id>-preview`, SKIPS the live deploy-signal + last-good persist; `persistFunctionsBundle` now runs on EVERY live deploy → R2 `functions-bundles/<id>.js`; `readFunctionsBundle`+`functionsBundleKey` feed the preview source. (ii) `functions_dispatch.ts` `resolveFunctionsDispatch`+`maybeDispatchFunctions` detect `?_ps_preview=1` → dispatch `site-<id>-preview`, SKIPPING the live deploy-signal gate (still entitlement-gated, still fail-soft). (iii) `api.ts` `POST /api/sites/:id/test-publish` — authed, org-scoped (404 cross-org), 409 when no persisted bundle, redeploys the last-good bundle to the preview slot, audits `functions.test_publish`. (iv) tests across all 3 dispatch/deploy specs. **Verify nuance (durable):** authed POST to the platform host `projectsites.dev` is CF-managed-challenged for non-browser clients (curl → 403 "Just a moment…"; `/api/*` is NOT in the `/api/mcp`+`/oauth/*` WAF-skip set) → route authed prod-verify POSTs through the worker's **`project-sites.manhattan.workers.dev`** URL (bypasses the zone bot-fight; same worker/D1/auth); child-host public GETs are NOT challenged. Bundle seeded for the verify by re-uploading northstar's OWN live WfP script content to R2 (persist-on-publish predates this deploy for northstar; the next real publish persists organically). — Original: Deploy the current `functions/` to a **preview** script `site-<siteId>-preview` (separate from live) that the owner hits to test before a real Publish promotes it. **STAGE 2 COMPLETE except the tracked fast-follow 2.2(d) bolt-path.**

## STAGE 3 — Dispatch in `site_serving`
- [x] **3.1 Reserved-path guard + dispatch.** — done `ae16fe686` (wiring live + prod-verified). In `site_serving`: for a child host (subdomain AND custom domains), if the path matches a reserved platform prefix (`/api/contact-form/*`, `/api/_ps/*` — enumerate + reserve the current platform set) → existing handler; else if `/api/*` + entitled + a deployed script exists → `env.USER_DISPATCH.get('site-<siteId>').fetch(request)`; else → R2 static / 404.
  - **PARTIAL — (core) decision logic done `f68e595a6`**: `src/services/functions_dispatch.ts` `resolveFunctionsDispatch({pathname, siteId, entitled, hasDeployedScript})` → `{action:'reserved'|'dispatch'(+scriptName)|'passthrough'}`, encoding the exact ADR §30 ordering: non-`/api/*`→passthrough; reserved prefix→reserved (platform wins, user fn ignored); non-reserved `/api/*` + entitled + deployed→dispatch to `site-<id>`; else→passthrough (404 not 403). PURE (no env/DB) — reuses the `isReservedFunctionRoute` + `siteFunctionsScriptName` SSOT so runtime dispatch can't drift from the build-time collision guard. 7 tests (incl. substring-safety `/api/eventsy`, charset normalise); suite 10887 green; typecheck + validate:features clean. Ships dormant (nothing imports it live yet).
  - **PARTIAL — (signal) `hasDeployedScript` done `3962005a1` (prod-verified)**: migration `0613_sites_functions_deployed_at.sql` = additive `sites.functions_deployed_at TEXT`, **applied to prod D1 `project-sites-db-production`** (`PRAGMA table_info` confirms the column, `success:true`). `functions_deploy.ts` gains `recordFunctionsDeploy(db, siteId, deployed)` (ISO ts on deploy / NULL on remove), wired into `deploySiteFunctions` best-effort + fail-soft (a signal-write failure never fails the publish), plus `siteHasDeployedFunctions(db, siteId)` (soft-delete-excluded read the dispatch guard consumes). deploySiteFunctions now PERSISTS its deploy state, not just fires to WfP. 5 new tests (12 total in the deploy spec); suite 10892 green; typecheck + validate:features clean. Column ships ready; the WRITE goes live with 2.2(a), the READ with the wiring below.
  - **DONE — (wiring) `ae16fe686` (deployed worker v`8c58c33f` + prod-verified)**: dispatch is wired in the `app.all('/api/*')` soft-404 guard in `index.ts` — **NOT** the site-serving catch-all. The `/api/*` guard fires for `/api/*` on ALL hosts and precedes the catch-all, so the catch-all was unreachable for `/api/*` (a first placement there was dead code — **caught by prod-verify**, not tests: the child `/api/*` returned the platform "Unknown API route" 404, proving the catch-all block never ran). For a child-site host the guard does `resolveSite` → `maybeDispatchFunctions(env, {siteId, orgId}, request, path)` → dispatch to `site-<id>` when entitled + a script is deployed, else the existing "Unknown API route" 404; platform hosts (base/www/localhost) skip it entirely (no extra resolve). `maybeDispatchFunctions` (functions_dispatch.ts) is the impure orchestrator: cheap pre-gate (non-`/api/*`/reserved → 0 D1 reads), deployed-script gate BEFORE entitlement (a site with no functions worker → 1 D1 read → passthrough), fail-soft (any error → null → static serving). 7 wiring tests (14 across the 2 dispatch specs); suite 10899 green; typecheck + validate:features clean. Prod-verified: `megabytespace.projectsites.dev/` → 200, child `/api/*` → clean 404 (dispatch path runs → null → 404, **not** 500), base domain + `/api/health` unchanged. No-op live until 2.2(a) deploys a script — the positive end-to-end dispatch (a real `functions/` worker returning a response) is exercised then. **STAGE 3 COMPLETE.**

## STAGE 4 — Binding injection + runtime
- [x] **4.1 Bindings — ✅✅ COMPLETE 2026-08-30 (all five bindings DONE + VERIFIED LIVE + isolation/metering/reconciliation-proven):** (a) `env.SECRETS` `4d3945ee2` · (b) `env.KV` `b7d102673` · (c) `env.R2` `45728547c` · (d) `env.AI` `10307b2e0` · (e) `env.DATA` `544d0f374`+`2034d42ab`. Every binding reaches platform data through a tenant-scoping SHIM (never a raw WfP binding): SECRETS = script-scoped `secret_text`; KV = `site:<id>:`-prefixed shared namespace; R2 = `sites-data/<id>/`-prefixed `SITES_BUCKET`; AI = metered debit-then-call; DATA = read-only signed-token internal fetch — all `__PS_*` internals stripped from the user env. The 4.1 design-note mandate (SHIMS, not raw bindings) is satisfied end-to-end. Inject `env.AI` (Workers AI, **debits `ai_credits_balance`**, clear error when out), `env.DATA` (typed read-only helpers `forms.list({limit})` + `site()`, tenant-scoped), `env.KV` (per-site namespace), `env.R2` (per-site prefix), `env.SECRETS.<KEY>` (site+org env-vars, site wins).
  - **⚠️ DESIGN NOTE — do NOT shortcut with raw WfP bindings.** A raw `{type:'kv_namespace'|'r2_bucket'|'ai'}` binding in the upload metadata would grant the user worker FULL-namespace KV + FULL-bucket R2 (**cross-tenant read/write**) + **unmetered AI** (no `ai_credits_balance` debit) — a security + billing hole. 4.1 MUST inject **tenant-scoping SHIMS** bundled via codegen: `env.KV` = siteId-prefixed over a shared namespace, `env.R2` = site-prefixed over `SITES_BUCKET`, `env.AI` = debit-then-call, `env.DATA`/`env.SECRETS` = read-only scoped. The shims reach platform data via a service binding OR a signed-token internal fetch to a platform `/api/_ps/data/*` endpoint. That internal contract + shim wiring are decided WITH 2.2(a) (a deployed worker is needed to verify scoping), so **4.1 is gated on 2.2(a)** — build the shims, never raw bindings.
  - **PARTIAL — (a) scoped-env scaffold + `env.SECRETS` — ✅✅ DONE + VERIFIED LIVE END-TO-END 2026-08-30** (`4d3945ee2`; deployed via container-deploy CI `33328792977`). **PROOF (org-brian-001, paid):** set site env-var `PROBE_KEY=hush-ada-42` via `/api/env-vars` (encrypted at rest) → `POST /api/sites/f84f5ab1…/test-publish` (deploy resolved+decrypted it, injected the binding) → child `northstar-functions-lab-sf.projectsites.dev/api/secret-probe?_ps_preview=1` → **200 `{ok:true,probe:"hush-ada-42",keys:["PROBE_KEY"]}`** — the env-var round-tripped through decrypt → binding → runtime shim → `env.SECRETS.PROBE_KEY` in user code; `keys:[…]` = exactly the one var (tenant-scoped, no leak). 99 tests green; whole-tree `tsc`=0. **Impl (4 files):** (i) `functions/runtime.ts` `buildFunctionsEnv(env)` — the scoped-env SEAM every handler flows through: parses the deploy-time `__PS_SECRETS_JSON` `secret_text` binding into a FROZEN `env.SECRETS`, STRIPS internal `__PS_*` bindings (never reach user code — the same seam later hosts the (b)-(e) KV/R2/AI/DATA tenant-scoping shims), fail-soft on a malformed/absent blob. (ii) `ai_env_vars.ts` `resolveEnvVarsForFunctions(env,orgId,siteId)` — org→site merge (site wins), NO `exposed_to_ai` filter (owner code gets ALL its env-vars, unlike the AI path). (iii) `wfp_dispatch.ts` `uploadSiteFunctionsWorker` accepts `secretsJson` → one `secret_text` binding `__PS_SECRETS_JSON`. (iv) `functions_deploy.ts` `deploySiteFunctions` resolves+injects on BOTH live + preview slots, fail-soft (resolve failure deploys without secrets, never blocks publish). `env.SECRETS` needs NO tenant-scoping shim — each script gets ONLY its own site+org secrets as script-scoped bindings (inherently isolated). Verify reused the 2.3 preview slot (seed bundle → test-publish → hit `?_ps_preview=1`); artifacts cleaned after (key/env-var/preview-script/R2-bundle).
  - [x] **(b) `env.KV` — ✅✅ DONE + VERIFIED LIVE + ISOLATION-PROVEN 2026-08-30** (`b7d102673`; deployed via container-deploy CI `33330800503`). **PROOF (org-brian-001, paid):** provisioned a shared account KV namespace `project-sites-user-functions` (`4cb559cb6d544cbd8be7c46da11f576b`, exposed as the `wrangler.toml` prod var `FUNCTIONS_KV_ID`); deployed a KV-probe function via the Stage 2.3 preview slot → `northstar-functions-lab-sf.projectsites.dev/api/kv-probe?_ps_preview=1` → **200 `{ok:true,val:"kv-value-42",keys:["probe-key"]}`** (`env.KV.put`→`get` roundtrips; `list()` STRIPS the site prefix → the owner sees the clean key `probe-key`). **★ ISOLATION PROVEN at the STORAGE layer:** `wrangler kv key list` on the shared namespace shows the key physically stored as **`site:f84f5ab1…:probe-key`** — a different site's `env.KV.get('probe-key')` resolves `site:<otherId>:probe-key` (absent) → it CANNOT read another tenant's data. 137 functions/wfp tests green (scoped get/put/list-strip + 2-site disjoint prefixes + env.KV wiring + binding injection); whole-tree `tsc`=0. **Impl:** `functions/runtime.ts` `makeScopedKV(kv,siteId)` — `get`/`put`/`delete`/`getWithMetadata` prefix `site:<id>:`; `list()` scopes to the prefix AND strips it from returned names — wired into `buildFunctionsEnv` from `__PS_KV` + `__PS_SITE_ID` (both stripped from the user env). `wfp_dispatch.ts` `uploadSiteFunctionsWorker` now ALWAYS injects `__PS_SITE_ID` (plain_text — the isolation boundary the KV/R2 shims key on) + `__PS_KV` (kv_namespace) when a `kvNamespaceId` is given. `functions_deploy.ts` reads `FUNCTIONS_KV_ID` via a narrow cast (`env.ts` was concurrent-dirty this pass) → passes `kvNamespaceId` on both live + preview. **The shim is the tenant boundary — NOT a raw namespace binding (per the 4.1 design note).** KV namespace creation is autonomous (two-way door); verify artifacts (key/probe-KV-key/preview-script/R2-bundle) cleaned. — Original: per-site key-prefix shim over a shared KV namespace so a site can't read another's keys.
  - [x] **(c) `env.R2` — ✅✅ DONE + VERIFIED LIVE + ISOLATION-PROVEN 2026-08-30** (`45728547c`; deployed via container-deploy CI `33331629121`, ~19min slow remote build). **PROOF (org-brian-001, paid):** an R2-probe function via the Stage 2.3 preview slot → `northstar-functions-lab-sf.projectsites.dev/api/r2-probe?_ps_preview=1` → **200 `{ok:true,val:"r2-value-77",keys:["probe.txt"]}`** (`env.R2.put`→`get().text()` roundtrips; `list()` STRIPS the prefix → the owner sees the clean key `probe.txt`). **★ ISOLATION PROVEN at the STORAGE layer:** the object is physically stored at **`sites-data/f84f5ab1…/probe.txt`** in `project-sites-production`, and the bare unprefixed `probe.txt` **DOES NOT EXIST** ("The specified key does not exist") — a site can never reach another site's (or the platform's) objects. 146 functions/wfp tests green (scoped get/put/head/delete + array-delete + list-strip of keys+delimitedPrefixes + 2-site disjoint prefixes + env.R2 wiring + binding injection); whole-tree `tsc`=0. **Impl:** `functions/runtime.ts` `makeScopedR2(bucket,siteId)` — `get`/`put`/`head`/`delete` prefix `sites-data/<id>/` (delete maps arrays too); `list()` scopes to the prefix AND strips it from returned object keys + `delimitedPrefixes` — wired into `buildFunctionsEnv` from `__PS_R2` + `__PS_SITE_ID` (both stripped from the user env). `wfp_dispatch.ts` `uploadSiteFunctionsWorker` adds the `__PS_R2` (r2_bucket) binding when a `r2BucketName` is given. `functions_deploy.ts` reads `FUNCTIONS_R2_BUCKET` via the narrow cast (`env.ts` concurrent-dirty) → passes it on live + preview. `wrangler.toml` `FUNCTIONS_R2_BUCKET="project-sites-production"`. **★ Design refinement (rationale): REUSES `SITES_BUCKET` prefix-isolated rather than a dedicated bucket — the `sites-data/<id>/` facade + the `__PS_` strip ARE the tenant boundary (user code only ever sees the facade, never the raw bucket), and this avoids the approval-gated new-R2-bucket jurisdiction one-way-door (per data-residency-by-default).** Verify artifacts (key/probe-object/preview-script/R2-bundle) cleaned. — Original: per-site object-prefix shim over `SITES_BUCKET` (`sites-data/<siteId>/` prefix).
  - [x] **(d) `env.AI` — ✅✅ DONE + VERIFIED LIVE + METERED-DEBIT-PROVEN 2026-08-30** (`10307b2e0`, service-binding fix atop `40ec571c2`; deployed via container-deploy CI `33334136964`). **PROOF (org-brian-001, paid):** an AI-probe function via the Stage 2.3 preview slot → `northstar-functions-lab-sf.projectsites.dev/api/ai-probe?_ps_preview=1` → **200 `{ok:true,result:{response:"…",usage:{total_tokens:58,neurons:0.88}},diag:{has_ai:true,has_secrets:true,has_kv:true,has_r2:true}}`** — `env.AI.run('@cf/meta/llama-3.1-8b-instruct-fp8',{prompt})` ran real Workers AI through the metered plane, and all four scoped bindings (AI+SECRETS+KV+R2) coexist in one handler. **★ METERING PROVEN at the STORE:** `ai_credits_balance` for org-brian-001 went **2077 → 2076** — exactly `AI_CALL_COST=1` debited per call (a 402-out-of-credits or a 502-AI-fault is NEVER charged). **★ 522 DISCOVERY + FIX (the load-bearing lesson):** the first cut (`40ec571c2`) had the shim `fetch()` the platform's PUBLIC `workers.dev` URL (`__PS_FN_URL`) — but a WfP **dispatch-namespace user script cannot fetch the platform's own workers.dev** (same-account reentry → **HTTP 522**), so the endpoint was never reached and no credit was debited (route was live: bogus token → 401, balance unchanged). Fixed by switching transport to an in-process Cloudflare **SERVICE binding** `__PS_SVC` → `project-sites` (no edge, no reentry, no bot-fight). 188 functions/wfp/credits tests green; whole-tree `tsc`=0. **Impl (6 files):** `functions/internal.ts` — the signed internal-fetch plane: `signFunctionToken(secret,siteId)=<siteId>.HMAC-SHA256(siteId)` + constant-time `verifyFunctionToken`, and `handleFunctionAiRun` (POST `/api/_ps/ai/run`): verify token → `dbQueryOne` org → getBalance (402 if <cost, AI NOT called) → `env.AI.run` (502 if it throws, NOT debited) → `debitCredits` + fire-and-forget `maybeFireAlerts`. `functions/runtime.ts` `makeScopedAI(token,svc)` calls `svc.fetch('https://ps-internal/api/_ps/ai/run')` (host is a placeholder; the platform routes on the PATH) — wired into `buildFunctionsEnv` from `__PS_FN_TOKEN` + `__PS_SVC` (both stripped from the user env). `wfp_dispatch.ts` injects `__PS_FN_TOKEN` (secret_text) + `__PS_SVC` (`{type:'service',service:'project-sites'}`). `functions_deploy.ts` signs the per-site token when `FUNCTIONS_INTERNAL_SECRET` + `FUNCTIONS_INTERNAL_SERVICE` are set (fail-soft → no env.AI, never blocks publish). `index.ts` registers `/api/_ps/ai/run` before the `/api/*` catch-all. `wrangler.toml` `FUNCTIONS_INTERNAL_SERVICE="project-sites"`. **The token is the tenant boundary — user code never sees a raw `ai` binding (unmetered) NOR the token (`__PS_*` stripped), satisfying the 4.1 design note.** Verify artifacts (key/probe-bundle/preview-script/R2-bundle) cleaned. — Original: Workers AI, credit-metered: debits `ai_credits_balance`, clear error when out (a debit-then-call shim, NOT a raw `ai` binding).
  - [x] **(e) `env.DATA` — ✅✅ DONE + VERIFIED LIVE + STORE-RECONCILED 2026-08-30** (`544d0f374` impl + `2034d42ab` lying-empty fix; deployed via container-deploy CI `33335843400` + `33336312522`). **PROOF (org-brian-001, paid, `northstar-functions-lab-sf`):** a data-probe function via the Stage 2.3 preview slot → `northstar-functions-lab-sf.projectsites.dev/api/data-probe?_ps_preview=1` → **200 `{ok:true, site:{slug:"northstar-functions-lab-sf", business_name, business_address, status, created_at}, forms_count:1, forms_sample:[{form_name:"verify-probe", email, fields:{name:"Ada Probe", message:"env.DATA works"}, status, created_at}], diag:{has_data:true, has_ai:true}}`** — `env.DATA.site()` returns the site's OWN metadata (safe fields only, no internal columns); `env.DATA.forms.list({limit})` returns THIS site's `form_submissions` with the stored `payload` parsed into `fields` and visitor `ip_address`/`user_agent` WITHHELD; both coexist with `env.AI` in one handler. **★ LYING-EMPTY caught by prod-verify + STORE RECONCILIATION (the load-bearing lesson):** the first cut filtered `WHERE site_id = ? AND deleted_at IS NULL`, but **`form_submissions` has NO `deleted_at` column** (it is NOT soft-deleted — the rare exception to the all-tables-soft-deleted convention) → D1 errored → the error was swallowed by `dbQuery` into an empty result → `forms_count:0` for a site that HAD a seeded row. Reconciling display(0) vs the authoritative store(1 row) exposed it → dropped the `deleted_at` filter (`2034d42ab`); the test now guards `sql.not.toMatch(/deleted_at/)`. `env.DATA.site()` was unaffected (`sites` HAS `deleted_at`). This is the `verify-against-source-of-truth` + `swallowed-sql-error-masks-schema-drift` classes, live. 350 tests green; whole-tree `tsc` + eslint clean. **Impl (reuses the 4.1(d) token plane + `__PS_SVC` service binding with ZERO deploy-config change — the bindings are already injected):** `functions/internal.ts` — a `siteIdFromAuth` helper + `handleFunctionDataForms` (GET `/api/_ps/data/forms?limit=N` → verify token → siteId → `form_submissions WHERE site_id=? ORDER BY created_at DESC LIMIT` clamped [1,100] default 20 → safe shape) + `handleFunctionDataSite` (GET `/api/_ps/data/site` → `sites` safe fields, 404 if missing). `functions/runtime.ts` `makeScopedData(token, svc)` → `env.DATA.forms.list({limit})` + `env.DATA.site()` over `svc.fetch('/api/_ps/data/*')`, wired into `buildFunctionsEnv` alongside `env.AI` (same `__PS_FN_TOKEN` + `__PS_SVC`). `index.ts` registers both GET routes before the `/api/*` dispatch catch-all. **Read-only + tenant-scoped BY CONSTRUCTION — user code gets no raw SQL, no writes, no cross-site/org data (siteId comes from the signed token, never a client param).** Verify artifacts (key/seeded-submission/preview-script/R2-bundle) cleaned. — Original: typed read-only helpers `forms.list({limit})` + `site()`, tenant-scoped, via a signed-token internal fetch to a platform `/api/_ps/data/*` endpoint (no raw SQL, no cross-tenant).
- [ ] **4.2 Guardrails + helpers.** Optional auth helpers (`ctx.verifyOwnerSession()` + Turnstile-verify); default per-IP edge rate-limit + Turnstile opt-in; **~25 MB** body cap; limits **50 ms CPU/req · 50 subrequests · 100k req/day per site** → 429 past it.
  - [x] **(a) dispatch abuse guardrails (body cap 413 + per-IP rate-limit 429) — ✅✅ DONE + VERIFIED LIVE END-TO-END 2026-08-30** (`07f93b547` guardrails + rate-limit binding, `c93faeeb6` payloadLimit exemption; deployed via container-deploy CI `33337223568` + `33337396319`). **PROOF (org-brian-001 paid, `northstar-functions-lab-sf` preview slot, echo probe):** (1) **body cap** — a real-browser POST of a **26 MB** body to `…/api/echo?_ps_preview=1` → **413 `{"error":{"code":"PAYLOAD_TOO_LARGE","message":"Request body exceeds the 25 MB limit."}}`**; (2) **rate-limit** — 140 cache-busted GETs (Service-Worker BLOCKED) → **43×200 then 97×429 `{"error":{"code":"RATE_LIMITED",…}}` + `Retry-After: 10`** (cutoff at the 100/60s-per-IP limit); (3) **happy path** — a normal GET → 200 (dispatch works; the guardrails don't break it). 664 tests green; whole-tree `tsc` + eslint clean. **Impl:** `functions_guardrails.ts` (pure `isBodyTooLarge` + `rateLimitKey` + `FUNCTIONS_BODY_CAP_BYTES=25 MB`); `functions_dispatch.ts` `maybeDispatchFunctions` runs BOTH guards BEFORE `dispatchToUserWorker` — body-cap → 413, then a Cloudflare native `FUNCTIONS_RATELIMIT` binding (`wrangler.toml`, keyed `<siteId>:<ip>`, 100 req/60s, tunable, modeled on `OAUTH_RATELIMIT`) → 429; both emit a tenant-tagged `functions.rejected` Trace event (`reason` added to the `log.ts` `SAFE_FIELD_ALLOWLIST` so it renders in the Log Explorer), both FAIL OPEN when their mechanism is absent (dev). **★ CRITICAL OTHER HALF — `payload_limit.ts` exemption:** the global 256 KB `payloadLimitMiddleware` runs BEFORE dispatch, so a functions body >256 KB died before the guardrail — which BOTH made the 25 MB cap unreachable AND blocked the ADR §110 25 MB-upload allowance. Fix: exempt a **non-reserved `/api/*` on a SITE host** (not the platform hosts, not reserved contact-form/events/_ps) to the upload ceiling → the dispatch guardrail is now the authoritative 25 MB cap. **★★ DURABLE PROD-VERIFY LESSONS (all three cost a debug cycle):** (i) a generated site's **Service Worker intercepts child-host `/api/*` fetches** and returns `{"error":"offline"}` **503** under burst — MASKING the real server response; to prod-verify server-side dispatch behavior (rate-limit/guardrails) from a real browser, use `browser.newContext({ serviceWorkers: 'block' })`. (ii) **repeat GETs to one URL serve from the browser/edge cache** → the worker never sees them (a burst showed all-200); **cache-bust** every request (`&cb=<uniq>` + `{cache:'no-store'}`) to actually exercise a per-request limit. (iii) a child-host **large/authed POST is CF-bot-challenged for curl** (403 "Just a moment") → drive it from a real browser (the child GET is not challenged). Verify artifacts (key/echo-bundle/preview-script/R2-bundle) cleaned. — Original scope: default per-IP rate-limit + ~25 MB body cap → 429/413.
  - [x] **(b) opt-in ctx auth helpers — ✅✅ DONE + VERIFIED LIVE END-TO-END 2026-08-30** (`33cb07bb5`; deployed via container-deploy CI `33338372378`). **PROOF (org-brian-001 paid, `northstar-functions-lab-sf` preview slot, auth-probe function):** `GET …/api/auth-probe?_ps_preview=1` — **anonymous** → `{owner:{authenticated:false}, turnstile:{success:false}, has_helpers:{session:true,turnstile:true}}`; **with the real site-owner session** (`Authorization: Bearer <session for user-brian-001>`, a member of the site's org) → **`{owner:{authenticated:true, userId:"user-brian-001", orgId:"org-brian-001"}, turnstile:{success:false}}`** — `ctx.verifyOwnerSession()` extracted the Bearer token from the request → forwarded to `/api/_ps/auth/verify-session` → `getSession` → membership check → owner confirmed; `ctx.verifyTurnstile('bad-token')` called siteverify with the platform secret + mapped `{success:false}` (a real token needs a widget; the success path is unit-tested). 401 tests green; whole-tree `tsc` + eslint clean. **Impl (reuses the 4.1(d/e) signed-token + `__PS_SVC` plane, ZERO deploy-config change):** `functions/router.ts` `FunctionContext` gains optional `verifyOwnerSession?()` + `verifyTurnstile?(token)`; `functions/runtime.ts` `makeCtxAuthHelpers(token, svc, request)` + `extractSessionToken` (Bearer or `session` cookie), wired into the `context` from the RAW env's `__PS_FN_TOKEN` + `__PS_SVC` (both stripped from `context.env`); `functions/internal.ts` `handleFunctionAuthVerifySession` (verify site token → org → `getSession` → membership in THIS site's org → `{authenticated,userId?,orgId?}`; an unauthenticated caller is a valid `{authenticated:false}`, never an error) + `handleFunctionTurnstileVerify` (siteverify with `TURNSTILE_SECRET_KEY`, graceful `{success:false}` on missing secret / fault); `index.ts` registers both `/api/_ps/*` POST routes. **Endpoints are PUBLIC by default — the helpers are opt-in; both FAIL CLOSED (deny) on any platform-fetch fault.** Verify artifacts (key/session/auth-probe-bundle/preview-script/R2-bundle) cleaned. — Original: `ctx.verifyOwnerSession()` + a Turnstile-verify helper on the FunctionContext, public by default, via new `/api/_ps/auth/*` + `/api/_ps/turnstile/*` endpoints.
  - [ ] **(c) per-site 100k-req/day cap → 429 — ⏸ DEFERRED (blocked; documented decision) 2026-08-31.** A per-site DAILY TOTAL needs a stateful counter. The only real-time-correct CF mechanism is a Durable Object per site — but (1) a new DO class requires a migration on THIS worker, whose DO migration history is **documented-fragile** (wrangler.toml warns of Cloudflare `10074`/errored-tag history + multiple `[[migrations]]` blocks "intentionally omitted") → a bad tag blocks EVERY deploy (mine + concurrent sessions); (2) a single global DO adds a cross-region round-trip to every dispatch's hot path (poor latency for a global endpoint); (3) it can't be meaningfully prod-verified (can't send 100k). The live per-IP rate-limit (4.2a: 100/min/IP) + per-invocation CPU/subrequest caps (4.2d) already bound abuse; a coarse daily TOTAL is lower-value + higher-cost/risk. **Two future paths when usage justifies it:** a per-site DO counter (in a SAFE migration window), or an Analytics-Engine count + a cron that flips a per-site KV "over-cap" flag (enforced by a cheap hot-path KV read). Per the loop guardrail (blocked AC → surface + advance a different AC), advanced to 4.2(d).
  - [x] **(d) per-invocation CPU + subrequest caps — ✅✅ DONE + VERIFIED LIVE 2026-08-31** (`40e2adc97`; deployed via container-deploy CI `33358899255`). Applies Cloudflare's **WfP custom limits at DISPATCH** (the CF-documented mechanism — `USER_DISPATCH.get(name, {}, { limits: { cpuMs: 50, subRequests: 50 } })` in `wfp_dispatch.ts`), NOT a script-metadata/upload change, NO DO, NO migration. `FUNCTIONS_DISPATCH_LIMITS = {cpuMs:50, subRequests:50}` in `functions_guardrails.ts` (ADR §10; tunable per plan). **PROOF (org-brian-001 paid, northstar preview slot, limit-probe):** `GET …/api/limit-probe?_ps_preview=1` → **200** `{ok:true,burned:false}`; the SAME endpoint with `&burn=1` (a ~2e9-op CPU loop, ~2–3s CPU — far past the 50 ms cap, but well under the 5-min paid DEFAULT) → **TERMINATED → 404** (the limit killed the invocation; the fail-soft dispatch_error path passes through to the platform's Unknown-API-route 404). Without the cap the loop would COMPLETE (200 `burned:true`) → the 404 proves the 50 ms cap fired. 340 tests green (incl. `.get()` called with the limits + the constant); tsc + eslint clean. **⚠️ workers-types gotcha: the installed `DispatchNamespace.get()` types 1–2 args but the runtime accepts a 3rd `options` (custom limits) — widen the BINDING (not the method, to preserve `this` per the illegal-invocation class), never the method reference.** Verify artifacts (key/probe-bundle/preview-script/R2-bundle) cleaned. **Minor DX follow-on (noted): a limit-breach currently surfaces as a fail-soft 404 (passthrough); a cleaner 503 "function exceeded resource limits" is a small future polish.**

## STAGE 5 — Versioning + observability
- [ ] **5.1 Snapshot versioning.** — **CODE LANDED 2026-08-31 (`3660e4241`); deploy (CI container image) + end-to-end prod-verify remaining.** Snapshot capture includes `functions/`; snapshot restore re-deploys that snapshot's WfP script (front + back roll back together). **Impl (TDD-first, 13 new tests RED→GREEN; 48/48 in the 2 specs, 256/256 across functions/snapshot/wfp; whole-tree `tsc`=0; eslint 0-errors; validate:features PASS):** the live functions bundle was stored at ONE key `functions-bundles/<siteId>.js` (overwritten every publish) → restoring an OLD snapshot would get the WRONG (current) functions. Fix = **per-build-version freeze + version-aware restore.** (i) `functions_deploy.ts`: `functionsBundleVersionKey(siteId, version)` → `functions-bundles/<siteId>/v/<version>.js` (the frozen per-build copy); `freezeFunctionsBundleForSnapshot(env, siteId, version)` copies the live bundle → the version key (no-op when the site has no functions; fail-soft); `restoreSnapshotFunctions(env, {siteId, orgId, version})` — entitlement-gated, reads the version-keyed frozen bundle → **present** = re-upload `site-<siteId>` to the LIVE slot (with the same tenant-scoped SECRETS/KV/R2/AI bindings a normal deploy injects, via a new shared `resolveFunctionsUploadBindings` helper refactored out of `deploySiteFunctions`) + refresh the live last-good bundle + set the deploy signal; **absent + live worker exists** = remove it + clear the signal (front+back roll back together); **absent + none live** = noop; NEVER throws. (ii) **Capture wired at BOTH snapshot-create sites**: `workflows/site-generation.ts` initial snapshot + `routes/api.ts` AI-edit auto-snapshot both call `freezeFunctionsBundleForSnapshot(…, version)` after the insert (fail-soft, dynamic import). (iii) **Restore wired**: `services/snapshot_restore.ts` `restoreSnapshot` calls `restoreSnapshotFunctions` after the successful content re-point (fail-soft — a functions rollback failure NEVER fails the content restore; result surfaced as `RestoreSnapshotResult.functions` + in the audit `metadata_json`). Reached in prod via `POST /api/sites/:siteId/snapshots/:snapshotId/restore` (`libs/features/site_versioning/handlers.ts:787` dynamic-imports `restoreSnapshot`). **REMAINING (external blocker — CI deploy + prod-verify):** worker deploy is a CI container-image build (`gh workflow run container-deploy.yaml -f target=production`); after green, prod-verify on `northstar-functions-lab-sf` (org-brian-001, paid): confirm a snapshot-create freezes `functions-bundles/<id>/v/<version>.js`, then delete the live `site-<id>` WfP script → `POST …/snapshots/:id/restore` → confirm `site-<id>` re-uploaded + `/api/hello` 200 again (front+back rolled back). Box checked with `— done <sha>` only after that dispatch is confirmed (rule 74). **No Dockerfile FORCE-REBUILD needed — all edits are worker-side TS (`src/services/`, `src/routes/`, `src/workflows/`), none are COPY'd container files (`container-server.mjs`/`scripts/**`/`src/services/functions/**`).**
- [ ] **5.2 Observability.** User-endpoint invocations + errors flow into the Log Explorer / Traces (`/admin/logs`).
  - **PARTIAL — (emit) dispatch Trace events done `fd1adb251`**: `maybeDispatchFunctions` (functions_dispatch.ts) now emits a tenant-tagged `functions.invoke` structured Trace event on every successful dispatch (`{level:'info', msg, orgId, siteId, scriptName, method, path, status, durationMs, cfRay}`) and a `functions.dispatch_error` event on failure — via `console.warn(JSON.stringify(...))` → Workers observability/Traces (console.log is ESLint-blocked). This ALSO fixes the prior silent-swallow (`catch {}` returned null with zero signal — an outage-hider per `graceful-degradation-hides-outages`). 3 new observability tests (10 in the wiring spec; suite 10902 green); typecheck + validate:features clean. **Committed dormant, NOT deployed** — a concurrent session held `api.ts`/`env.ts`/`app_js.ts`/`analytics_events.ts` dirty, so a deploy would ship their in-progress work; this goes live on the next clean deploy (behaviorally dormant anyway — no invocations until 2.2(a)).
  - **(align) canonical logger + mapEvent schema done `3a199bb64`**: verified the surface — `/admin/logs` reads **Workers Observability Telemetry** via `logs_explorer.mapEvent` (NOT a D1 table), which keys on `msg`/`method`/`path`/`status`/`durationMs`/**`requestId`** + ISO `ts` + `eventName`. Switched the hand-rolled `console.warn(JSON.stringify(...))` (wrong `cfRay` field → mapEvent ignored it; numeric `ts`) to the canonical `log.child('functions')` logger, so events carry ISO `ts` + `eventName` alias + PII-redaction + the exact field names mapEvent renders. Dropped `scriptName` (derivable from siteId; not in the logger's `SAFE_FIELD_ALLOWLIST`). Invocations now render as real Log-Explorer rows, filterable by siteId (in `meta`) / free-text `functions.invoke`. 10 wiring tests (schema-alignment assertions incl. `requestId`); suite 10907 green; typecheck + validate:features clean. **Observability mechanism COMPLETE + schema-verified.**
  - **REMAINING (external blockers only, no more code)**: deploy (a concurrent session still holds `api.ts`/`env.ts` dirty → can't deploy without shipping their WIP; goes live on next clean deploy) + end-to-end prod-verify a real invocation rendering in `/admin/logs` (needs 2.2(a) to produce invocations). Optional future: the USER worker's OWN `console`/errors → its per-script traces (per-script observability at upload, Stage 4.x).

## STAGE 6 — Fast-follow (after v1 core ships)
- [ ] **6.1 Scheduled/cron handlers.** `functions/_scheduled.ts` with a cron expression (WfP per-script cron) — the most-powerful option, sequenced after the HTTP path is green.

---

## Convergence rules
- Pick the **lowest-numbered incomplete AC** whose dependencies are met. Stages 0→5 are ordered; 6 is last.
- Every AC: TDD-first · typecheck · unit · build · deploy · **prod-verify the real surface** · update this ledger (check box + `— done <commit>`) · commit + push. A toast/compile is not proof.
- **Safety gates (approval-required — pause + surface, don't auto-run):** the `DROP TABLE ai_endpoints` prod migration (destructive — pre-cleared as test-data-only, but confirm the count is still ≤6 test rows immediately before) and any WfP dispatch-namespace provisioning that needs CF account setup.
- When **all** ACs are checked: run a completeness + zero-recommendations pass; if genuinely done, mark **Status: ✅ complete** and report DONE. Do not manufacture further work.
- Log each pass in the `always.md` end-of-turn report format (journey/AC/change/verify/next + the ⚡ block).
