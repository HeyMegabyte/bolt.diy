# LiveKit Cloud agent config. `lk agent create` normally generates this; committed
# here so the agent project is self-describing. `subdomain`/`id` are filled by
# `lk agent create` on first deploy (it writes back the generated agent id).
[project]
subdomain = "projectsites-dev-1ydfdbtm"

[agent]
# Must match WorkerOptions.agentName in src/agent.ts and the LiveKit SIP dispatch rule.
id = "CA_dSUDxEC3EiP6"
name = "projectsites-receptionist"
