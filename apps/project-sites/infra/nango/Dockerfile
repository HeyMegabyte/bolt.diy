# Nango — unified integrations/OAuth gateway for integrations.projectsites.dev on
# CF Workers Containers. Self-hosted OSS server image (NOT the managed/Cloud variant).
# State in Neon Postgres + Upstash Redis. DB migrations run on boot.
# nango-server:hosted is the SELF-HOSTED open-source server. nango:managed-* is Nango
# Cloud platform (wrong CMD, wrong migration set — don't use for self-host).
FROM nangohq/nango-server:hosted-0.70.9
