# Nango — unified integrations/OAuth gateway for integrations.projectsites.dev on
# CF Workers Containers. State in Neon Postgres (standard tables, no pg_authid —
# Neon-compatible, unlike Logto). The image runs DB migrations on boot.
FROM nangohq/nango-server:hosted@sha256:61b380cdde637f02541b8ed842070d798f9983fccaedae9976b6a84ea2aa4709