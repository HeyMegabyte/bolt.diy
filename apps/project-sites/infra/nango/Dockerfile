# Nango — unified integrations/OAuth gateway for integrations.projectsites.dev on
# CF Workers Containers. Self-hosted open-source server image.
# State in Neon Postgres + Upstash Redis. DB migrations run on boot.
# Exposed port 8080; server reads SERVER_PORT from env.
FROM nangohq/nango-server:hosted-0.70.9
