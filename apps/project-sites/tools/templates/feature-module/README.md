# {{Name}} (`{{slug}}`)

> Auto-generated by `npm run gen:feature -- --slug {{slug}} --name "{{Name}}"`.
> Edit this file to document the feature's purpose, surfaces, and lifecycle.

## Description

TODO: one paragraph description of what {{slug}} does.

## Flag key

```
{{SLUG_UPPER}}
```

Default state: `enabled=false, rollout_percent=0, stage=experimental`.  
Admin UI: `/admin/feature-flags` → search `{{slug}}` → toggle / promote.

## API surface

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| GET | `/api/{{slug}}` | Bearer | List resources |
| POST | `/api/{{slug}}` | Bearer | Create resource |

## UI surface

| Path | Guard |
|------|-------|
| `/admin/{{slug}}` | `useFeatureFlag('{{SLUG_UPPER}}')` |

## Enabling

1. Admin UI → `/admin/feature-flags` → `{{slug}}` → toggle **Enabled** to on.
2. Set **Rollout %** to 5 and watch error rate for 1 week.
3. Promote: `experimental` → `beta` → `stable`.

## Killswitch

Admin UI → `{{slug}}` → **Stage** → `killswitch`.  
Instantly disables for all users without a redeploy.

## Removal (after 30+ days at stable 100%)

1. Delete `libs/features/{{slug}}/`.
2. Remove `{{SLUG_UPPER}}` entry from `src/modules/feature_flags/registry.ts`.
3. Remove `{{SLUG_UPPER}}` entry from `src/modules/feature_flags/docs.ts`.
4. Unmount routes from `src/index.ts`.
5. Drop any D1 migrations that are {{slug}}-only (add notes to migration header).

## Owner

{{owner}}
