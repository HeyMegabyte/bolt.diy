# Convergence State — projectsites.dev

> Durable brain for the `.claude/loop.md` convergence loop. Convergence MAP, not a journal.
> Reload at the start of every iteration. Statuses: UNREVIEWED · AUDITING · REFACTORING · VERIFYING · CONVERGED · BLOCKED.
> **Per-iteration detail lives in git history — the commits ARE the journal. This file is the MAP.** (Compressed iter 25; iters 1-24 folded into "Converged arcs" below.)

## Repo shape (authoritative)
- **Worker** (`apps/project-sites/src` + `libs/features/*`) — CF Workers + Hono + D1/R2/KV/DO. Jest (`.cjs`, `@swc/jest`). Run from `apps/project-sites` (NOT repo root → babel trap). `npm test` ≈ 670 suites / ~10.7k tests.
- **Frontend** (`apps/project-sites/frontend`) — Angular 21 standalone, signals, Karma+Jasmine units, Playwright E2E. Spartan/helm partial. `npm run build:prod`, `npm run deploy:production` (R2).
- **Feature modules** `libs/features/<slug>/` — manifest (`feature.manifest.ts` OR `manifest.ts`) + flag. Gates: `validate-feature-drift.mjs` + `validate-feature-manifests.mjs` (both have `GROUPED_FLAG_KEYS` + `__core__` exemptions).
- **Flags**: `src/modules/feature_flags/registry.ts` (runtime SoT) mirrors `docs.ts`. Prod D1 `feature_flags` keys on `flag_name` (NOT `key`); overrides on `flag_key`. After a flag change, re-check for constant-based `FLAG_KEY` drift.
- ⚠️ Deploy worker needs Docker + GLOBAL CF key (`get-secret CLOUDFLARE_API_KEY`, email `blzalewski@gmail.com`). Frontend = R2, no Docker. **ALWAYS pass `--config wrangler.toml`** — bare `wrangler deploy` misfires monorepo framework auto-detection (pnpm workspace). `npx wrangler deploy --env production --config wrangler.toml`.
- ⚠️ **Shared working tree, 7+ concurrent sessions + crons.** Stage EXPLICIT paths, never `git add -A`. `pull --rebase --autostash`. Concurrent sessions periodically prettier-reformat my just-shipped files (clean fast-forwards). Sidebar/admin-shell BLOCKED (concurrent session owns `admin.component.*` + `nav-icon` + `admin-navigation-responsive.e2e.ts`).

## Subsystem status
| Subsystem | Status | Notes |
|---|---|---|
| feature flags | CONVERGED | 54→41 grouped (7 anchors) + 26 removed; registry↔docs parity (iter 2). iter 36: hardened `invalidateFlagCache` (services.ts:179) — it deleted only the FIRST KV `list` page (≤1000 keys), so an admin toggle of a flag cached across >1000 `(siteId,orgId)` scopes left later scopes stale until their 60s TTL lapsed (laggy toggle for large tenants). Now walks the cursor across every page (`list_complete !== false` terminates; missing field ends the sweep so existing single-page mocks don't infinite-loop). All 3 override-write paths (super_admin:966/features:507/copilot:251) VERIFIED to call it — the older "override-write skips invalidateFlagCache" bug is fixed. +1 regression test (multi-page cursor walk). Deploy `78ae70d8`, tsc 0, 125/125 flag-suite tests. |
| worker test suite | CONVERGED | 670/670 suites green (iters 1-3). |
| data access (`db.ts`) | CONVERGED | dbInsert + dbUpdate SYMMETRIC (both learn-and-retry the timestamp fallback). iter 17 data-loss fix. |
| team/ownership (`team_seats.ts` + ai_admin transfer) | CONVERGED | atomic transfer (18) + soft-delete-scoped owner gate (23). |
| domains/hostnames (`domains.ts`) | CONVERGED | atomic `setSolePrimary` swap (19). Site-serving host resolution filters deleted_at (verified 24). |
| site branches (`site_branches.ts`) | CONVERGED | atomic cross-table `mergeBranch` (20). |
| account deletion (`api.ts DELETE /api/admin/account`) | CONVERGED | atomic 3-table soft-delete batch (21). |
| soft-delete filters (authz) | CONVERGED | 2 membership authz bugs fixed (23-24); sessions/site-serving/subscriptions verified clean. |
| notification recipients (soft-delete) | CONVERGED (iter 35) | Owner-email-resolution class SWEPT: 5 instances added `m.deleted_at IS NULL AND u.deleted_at IS NULL` — a removed member (soft-deleted membership) or soft-deleted user could receive build emails / confer free compute. Fixed: `site-generation.ts:1505/1511` (notifySiteBuilt org+member lookup), `abandoned_builds_cron.ts:57` (nudge recipient subquery), `notify_site_built.ts:27` (`resolveOwnerEmail` — "your site is live" email), `notify.ts:80` (`notifySiteOwner` — psnotify webhook/workflow), `build_limits.ts:44` (`isUnlimitedOrgOwner` — removed owner conferred UNLIMITED compute, a cost bug). Already-CLEAN refs (correct pattern, don't touch): `weekly_digest.ts:338`, `auth_org.ts:66`, `wallet.ts:293`, `env_vars.ts:138` (`isOrgOwner`), `ai_admin.ts:1364` (`/api/team`). `ai_admin.ts:2051` INTENTIONALLY unfiltered (admin full-fidelity data dump). 5 regression tests lock the SQL string. Deploys `f81f235a`→`ff7383b8`. tsc 0, jest 45/45. |
| billing/entitlements | CONVERGED | `getOrgEntitlements` requires paid+active+`deleted_at IS NULL` (verified 25); subs never soft-deleted → status-based lifecycle correct. |
| voice conversations (FE↔worker) | CONVERGED (feature CLOSED) | iters 26-28 closed the feature. 26: detail transcript route. 27: `GET .../download.{txt,vtt,mp3,mp4}` (txt/vtt from `transcript_json`; mp3/mp4 302→`/recordings/:id/stream`) + FE `download()` → authed blob fetch (`ApiService.getBlob`) — a plain `<a download>` can't send the Bearer. 28: detail route now looks up `voice_recordings` → returns `has_recording`/`has_video` so the FE enables the mp3/mp4 buttons + fetches the recording as an authed blob for `<audio>/<video>` playback (plain `<audio src>` also can't send the Bearer; object-URL bound + revoked on close/destroy). 13 jest + 21 karma green. Deploys 4cd5715b→a835e085→64cb3a0e. |
| authed media / bare-nav (FE) | CONVERGED (axis CLOSED) | The [[plain-navigation-cant-carry-bearer]] class beyond voice. iter 29: snapshot screenshots (plain `<img src="/api/…/screenshot.png">` authed→401 broken thumbnail + plain-nav download) → REUSABLE `AuthImageSrcDirective` (`img[appAuthImageSrc]` → `getBlobAbsolute` → object URL, revoked on change/destroy) + download via `getBlobAbsolute`. iter 30: docs-overview `target=_blank` link to the AUTHED `/api/admin/docs/openapi.json` → popup-safe `openRawSpec()` (open blank tab in the click gesture, fetch spec WITH auth, navigate tab to blob). 7 karma (5 directive + 2 openRawSpec). FE-only (`main-3FNGE2UK`→`main-QZAZPU6N`). AUDITED SAFE (don't re-open): social `<img [src]="m.thumb_url\|\|m.url">` = blob-URL local previews (no `/social/media` route → always the `URL.createObjectURL` fallback); OAuth `/api/auth/{google,github}` links (unauthenticated by design); public `/api/openapi.json` (200). |
| route-registration integrity | DETECTOR shipped; 1 of 3 review dups resolved | iter 31: found the duplicate-route class (two handlers for the same `METHOD /path` on `/`-mounted routers → first wins, second SILENTLY SHADOWED) + built `scripts/check-duplicate-routes.mjs` (`check:duplicate-routes` + fixture; allowlist grandfathers existing, blocks NEW). iter 32: resolved `GET /api/sites/:siteId/integrations` — it was a COLLISION not a dup: forms.ts (newsletter `{data}`, DEAD — 0 FE consumers) shadowed site_detail_tabs (MCP `{providers}`, LIVE — site-detail.component:847). Re-pathed the live MCP GET+DELETE → `/api/sites/:siteId/integration-providers` + **added the missing org-ownership guard** (un-shadowing exposed an IDOR) + updated the 2 FE call sites. Fixes site-detail integrations panel (was always showing DEFAULT_PROVIDERS + disconnect was a no-op). 21 jest, deploy `6d0ae5a9`+`main-YO4VFOW3`; verified new route 401/404-ownership. ⚠️ 2 review dups LEFT — iter 33 ANALYZED both: NOT quick removals, both need a DELIBERATE decision (one-way-door), NEITHER is a live bug (both FE-unconsumed). (a) `GET /api/admin/domains`: a COLLISION — api.ts:5838 is the RICHER/DOCUMENTED impl (flat `{data}` + pagination + status/type filters + curl example + 5 tests in domain_admin.test.ts) but is SHADOWED by the simpler ai_admin.ts:2329 (`{data:{sites}}`). Only `/admin/domains/summary` is FE-called → no live consumer disambiguates the canonical contract; resolving = an API-contract decision, not a delete. (b) `GET /`: index.ts:747 shadows :976 (superset); VERIFIED via prod curls that :976 is moot (logs/webhooks/links/analytics roots serve 200 via containers not :976; llm container-served; billing 404s regardless) → fix is DELETE :976 (a merge would HIJACK the 4 working platform hosts), but its helpers (llmLandingPage+resolvePlatformService+platformServiceLanding) are :976-only AND llm_landing_page has its own test → a focused host-routing dead-code slice. |
| detector gates (CI `check` chain) | CONVERGED (iter 34) | 2 gates were RED on FALSE POSITIVES (blocking the `check` chain): `check:unwired` flagged the ApiService generic verb helpers (`GET /*` etc.) + prefix-mounted routes (`app.route('/api/onboarding', X)` + relative `.post('/dismiss')` — a route-scanner blind spot) + built-ahead dark site-mcp; `check:no-openmeter` flagged the migration LEDGER + a "removal" comment + a legacy-value REJECTION (`=== 'openmeter'` that THROWS). Fixed both per validator-precision-discipline: unwired now skips `/*` + RESOLVES prefix mounts (strip prefix, match relative shape) + exempts site-mcp; no-openmeter exempts the ledger + removal/superseded/stale/quoted-value/self-ref. All 10 detectors green, test:scripts 33/33. Build-tool only, no deploy. ⚠️ the `check-duplicate-routes` detector has the SAME prefix-mount blind spot (only sees absolute registrations) — fine today (no `/api/*`-prefix dup) but note if it fires. |
| billing/wallet (`wallet.ts`) | AUDITING | debit path CORRECT (atomic). SURFACED (approval-tier): `creditWallet` double-credit — see SURFACED below. |
| media library (`routes/media.ts`) | CONVERGED (auth + user-content headers, iter 40) | Audited `/api/media/*`: auth/ownership CLEAN — `orgId` from `c.get('orgId')` (no header-IDOR); `/raw` calls org-scoped `getAsset` BEFORE `SITES_BUCKET.get(r2_key)` (no cross-org read); upload/delete/stock all org-scoped. **FIXED user-content XSS defense on `/api/media/assets/:id/raw`** — it streamed user-uploaded bytes with a caller-supplied MIME (upload trusts `file.type`, no allowlist) + ZERO security headers. Added `X-Content-Type-Options: nosniff`, a `default-src 'none'; sandbox` CSP, and `Content-Disposition` (INLINE for raster-image/AV `INLINE_SAFE_TYPES`, ATTACHMENT for svg/html/pdf/etc. so a malicious upload downloads instead of rendering as a document) + a sanitized filename (strips CRLF/quotes → no header injection). ⚠️ The sandbox CSP was CLOBBERED by `securityHeadersMiddleware` (its dashboard block set CSP unconditionally, contradicting its own "handler can override" doc) → **fixed the middleware to preserve a handler-set CSP** (`if (!c.res.headers.get('CSP'))`; media.ts is the ONLY route setting its own CSP → zero impact on others). Severity=defense-in-depth (endpoint is bearer-gated; bolt path uses R2-presigned URLs on a separate origin) but nosniff is a REQUIRED header + the endpoint shipped none. +5 tests (3 media, 2 middleware). Deploys `06aa6b8c`→`05462fda`; VERIFIED live authed: raw serves sandbox CSP + nosniff + disposition, homepage still global CSP. tsc 0, 93 tests. **iter 41: cost-shield rate limits on media GENERATION** — `/api/media/generate/{image,video,podcast}` (DALL-E / Sora-Veo / TTS — the most expensive paid-AI endpoints) had NO `RATE_LIMIT_RULES` entry + no quota gate → an authed org could flood them for unbounded provider cost. Added per-IP flood shields to the SSOT (`middleware/rate_limit.ts`): image 10/60, **video 3/60** (Sora/Veo is $$$/clip), podcast 8/60. +4 tests (auth-rate-limit.test.ts). Deploy `7b78083d`. ⚠️ can't curl-verify the 429 live — CF Bot-Fight-Mode challenges raw curl (`cf-mitigated: challenge`) BEFORE the worker (per `[[bot-fight-mode-blocks-inbound-webhooks]]`); the 21 unit tests + `applyRateLimits` wiring (index.ts:420) prove it. tsc 0, 21 rate-limit tests. |
| AI provider routing (`external_llm.ts`) | CONVERGED (DeepSeek routing fixed, iter 42) | **LIVE (latent) bug: the whole DeepSeek cost-tier was non-functional.** `callExternalLLM` dispatches `provider:'deepseek'` (reachable via explicit pref OR `tier:'standard'/'instant'` when `DEEPSEEK_API_KEY` set) to `callOpenAI` (correct — DeepSeek is OpenAI-wire-compatible), BUT `callOpenAI` HARDCODED `fetchWithGatewayFallback(env,'openai',…)` → a DeepSeek key POSTed to OpenAI's URL → 401 → 3 wasted retries + deepseek circuit-breaker pollution → silent fallback to PREMIUM OpenAI (defeating the documented mid-grade DeepSeek cost tier per `model-routing`). The gateway layer ALREADY supported deepseek (`ai_gateway.ts` `GatewayProvider` + `DIRECT_BASE_URLS.deepseek='https://api.deepseek.com'` + `DEFAULT_MODELS.deepseek`); external_llm just never threaded the provider through. Fix: widen `fetchWithGatewayFallback` provider type + add `provider` param to `callOpenAI` (default `'openai'`, so OpenAI/Anthropic/vision callers unchanged) + pass it from the dispatch. ⚠️ The pre-existing `external_llm_provider_tiers.test.ts` tested the CONFIG (chooseProviderForTier + base-URL constant) but NOT the ROUTING — so it stayed green while the dispatch was broken (config-vs-behavior blind spot). +2 behavioral tests (assert `gatewayFetch` called with `'deepseek'` + DeepSeek key). tsc 0, 53 external_llm tests. Deploy `23f3ada6`, prod 200. **iter 43: image-gen Stability fallback aspect-ratio bug** (followed the iter-42 lead to the SAME dispatch-blind-spot class). `workflows/image-generation.ts` DALL-E→Stability fallback ROUTES correctly (right URL `api.stability.ai/v2beta/stable-image/generate/core`, multipart FormData, skip-if-DALL-E-worked) BUT mapped a SQUARE request (`1024x1024` — the DALL-E default + most common: logos/icons/favicons/section images) via `width >= height ? '16:9' : '9:16'` → square → **16:9** (wide). So every square image that fell back to Stability came out wide-distorted. Fixed → `width === height ? '1:1' : width > height ? '16:9' : '9:16'` (Stability core supports `1:1`). ⚠️ `callDallE3`/`image_generation.ts` service = DALL-E only (no Stability), routing correct; the fallback lives ONLY in the workflow. +2 tests (square→1:1, tall→9:16; wide→16:9 already covered). tsc 0, 6 workflow tests. Deploy `15da90c6`, prod 200. |
| sidebar / admin shell | BLOCKED | Concurrent session owns it (per loop prompt). Do NOT touch. |
| Angular admin | VERIFYING | Spartan/helm partial; ag-grid→TanStack pending (`docs/perf-wave-ag-grid-to-tanstack.md`). |
| site-kit (Storybook lib) | CONVERGED (control-flow, iter 37) | 26 `frontend/src/app/site-kit/*` components (`sk-*`, generated-site building blocks, 26 `.stories.ts`) migrated deprecated `*ngIf`/`*ngFor` → `@if`/`@for` via the official `ng g @angular/core:control-flow --path=src/app/site-kit` schematic (removed in v22 → was a time-bomb). Schematic SMART: auto-removed now-unused `CommonModule` (net deletion) + added the SPECIFIC `CurrencyPipe` where `\| currency` is used (listings-grid/menu-board/pricing-tiers). +cleaned 20 stale `(*ngIf)` prose comments → `@if`. ⚠️ site-kit is **Storybook-ONLY** (0 imports in `src/app` → tree-shaken from admin bundle) → deploy surface = `storybook.projectsites.dev` **Cloudflare PAGES** (`npm run deploy:storybook`, NO Docker/R2), NOT the admin R2 slot. tsc 0, ng build 0 errors, Storybook built+rendered all 26; deployed Pages `04ff4dd5`, storybook.projectsites.dev 200. **iter 38: a11y-hardened `gallery-lightbox`** — the lightbox modal had NO focus management (WCAG 2.4.3): opening didn't move focus in, Tab escaped behind the overlay, closing didn't restore focus. Added self-contained (no app-directive coupling) focus-IN (rAF → close button), focus-TRAP (`onKey` Tab cycles the dialog's buttons + pulls stray focus back), focus-RESTORE (stored `triggerEl` refocused on close/Escape). +the FIRST site-kit Karma spec (5 tests, fixture body-attached for real focus). Deployed Pages `36e17cab`. site-kit spec coverage 0→1. **iter 39: `multi-step-form` validation + a11y** — the form declared required fields (visual `*` + `[required]`) but NEVER enforced them (no `<form>`, `type=button` nav) → `next()`/`submit()` advanced/emitted with empty required fields (silent incomplete leads). Added a required-field GATE (blocks advance/submit, `aria-invalid` + `role="alert"` per-field messages, clears on `ngModelChange`), an `aria-live` region announcing step changes + validation, and focus mgmt (first-invalid on error, first-field on step change). +7 Karma specs. ✅ **interactive-site-kit-a11y axis CLOSED** — gallery-lightbox + multi-step-form are the ONLY 2 site-kit components with click handlers (iter-37 grep); both now hardened. Deployed Pages `4edd1932`. site-kit spec coverage 1→2. |
| frontend health | CONVERGED | `build:prod` green (9.7s); 243 kB initial bundle; Karma **1617/1617** (iter 39; +5 gallery-lightbox + 7 multi-step-form a11y/validation specs). 2 tracked warnings below. ⚠️ iter 38 caught a STALE spec RED since iter 32: `site-detail.component.spec.ts:389` still asserted the OLD `/sites/s1/integrations/mailchimp` DELETE path after iter 32 re-pathed the code + FE call sites to `/integration-providers/` but MISSED the Karma assertion (frontend suite hadn't been run to completion since iter 4 → the regression hid). Fixed the assertion to the correct path. |
| referral_loop feature | AUDITING | Removal candidate (credits unwired). Test GREEN (iter 3). |
| everything else | UNREVIEWED | rotate per loop priority. |

## Converged arcs (don't re-audit without NEW evidence — full detail in `git log`)
- **#12 duplication** (iters 6-16): 1 real dead file deleted + 6 single-file DRYs + 11-module error-envelope→`feature_guard` consolidation. **6 detector gates** now guard convergence classes: fitness, idor, unwired, dbinsert, admin-email, local-error-helpers (`scripts/check-*.mjs` + fixtures + CI `feature-architecture.yml`).
- **Atomicity vein** (iters 17-22): 5 LIVE bugs — all "multi-step D1 mutation that should be atomic but wasn't + swallowed the `{error}`", each fixed with `db.batch([...])` (implicit txn, rejects+rolls-back). db.ts dbUpdate timestamp-fallback (data loss) · team_seats transferOwnership (ownerless org) · domains setSolePrimary (primary-less site) · site_branches mergeBranch (half-merged) · api.ts account-delete (half-deleted acct, sessions kept). Vein verified exhausted (22: remaining consecutive-mutation sites are non-bugs).
- **Soft-delete filters** (iters 23-24 authz + iter 35 notification-recipients): iters 23-24 = 2 LIVE membership authz bugs (transfer gate + impersonation org-scope). iter 35 = the notification-recipient / owner-email-resolution sub-class, 5 more instances (see subsystem row). Class total = 7 across 2 sub-classes. **STILL no detector — the false-positive risk is now CONFIRMED, not hypothetical:** of ~12 `memberships`-JOIN queries, ~half LEGITIMATELY omit `deleted_at` (admin member listings `/api/team`, the `ai_admin.ts:2051` full-fidelity data dump). A naive "missing deleted_at" grep would flag those correctly-unfiltered admin views → per `validator-precision-discipline` (prefer false-negatives), classify each by READING context (recipient/authz gate → MUST filter; admin listing/dump → correctly unfiltered), never by grep count.
- **Verified-CLEAN surfaces (do NOT re-hunt):** auth `getSession` (expiry+revocation), webhook dedup (`UNIQUE(provider,event_id)`), site-serving host resolution, subscription→entitlement gate, dead-code (knip).
- **SSRF class** (verified converged iter 41): `isSafeCrawlUrl`/`isSafeWebhookUrl`/`isSafePublicHost`/`isPrivateIPv4` (`outbound_webhooks.ts`) is CORRECT + comprehensive — blocks 0/8+10/8+127/8+169.254/16(metadata)+172.16-31+192.168/16+100.64/10(CGNAT), IPv6 loopback/ULA/link-local/`::ffff:`-mapped, `.local`/localhost; `new URL()` normalizes decimal/hex/octal IP encodings before it. 5+ guarded call sites (media stock-save, import_crawler w/ per-redirect re-check, search.ts image-proxy w/ 3-hop re-validate, image-search candidates, SES SNS, webhook delivery). Comprehensive bypass-vector tests (`outbound_webhooks.test.ts`: 0.0.0.0, CGNAT, `::ffff:`, metadata). ONLY gap = DNS-rebinding (documented + tracked; needs connect-time IP pinning Workers can't do). Do NOT re-audit.
- **Contact form** (`search.ts:1141` `POST /api/contact-form/:slug`, verified converged iter 41): Zod `contactFormSchema` + persist-first to `contacts` (error-checked) + `escapeHtml` every field + reply-to submitter + bell notify + fail-soft + **rate-limited** (`RATE_LIMIT_RULES` 5/60). CONVERGED.
- **Rate-limit SSOT** (`middleware/rate_limit.ts` `RATE_LIMIT_RULES` + `applyRateLimits` at index.ts:420): comprehensive per-IP budgets on auth/search/contact/feedback/create/forms/ai/bolt(vision/transcribe/suggest/chat)/oauth/mcp + now media-generate (iter 41). KV-counter, fails-OPEN on KV outage. When adding a public/cost endpoint, add its rule HERE (the test imports the array → no drift).
- **FE↔worker contract** (iters 26-27, SWEPT): a FE call to a NON-existent worker route (or response-key mismatch) silently shows empty. iter 26 fixed the missing `/api/voice/conversations/:id` (transcript detail). iter 27 SWEPT the rest — clean: onboarding (exists, flag-gated), posts/analytics, logs/tail, webhooks/deliveries, mcp/connections (`{data:{connections}}` matches both readers) all EXIST + match; api.service `/*` = generic-wrapper false-positives. ⚠️ The unwired-endpoint gate has false-negatives on FE dynamic paths that prefix-match a list route (missed voice/conversations/:id).
- **Incomplete surfaces (FE built, backend missing — focused-session builds, NOT cron slices):**
  1. **site-mcp tokens/tools/tool-usage** (feature #29, `/admin/sites/:id/mcp-server`): 5 endpoints missing (`GET/POST /mcp/tokens`, `DELETE /mcp/tokens/:id`, `GET /mcp/tools`, `/mcp/tool-usage`). Flag `site_mcp_server` is DARK (not in registry → 404s regardless), so the page is dark — NOT a live bug. Table `site_mcp_tokens` EXISTS (0514). Tractable core = tokens CRUD (assertSiteOwned + SHA-256 hash, raw shown once). Build ON promotion per CLAUDE.md's dormant-features guidance, NOT speculatively.
  2. **voice conversation downloads + playback** — ✅ DONE (iters 27-28), feature CLOSED: worker download route (txt/vtt/mp3/mp4) + FE authed-blob `download()` (27); detail route now looks up `voice_recordings` → `has_recording`/`has_video` so the mp3/mp4 buttons enable + `<audio>/<video>` play from an authed blob (28, `64cb3a0e`). Nothing left here. (⚠️ Aside, NOT a bug: the conversation LIST route omits `has_recording`/`has_video`, so the list REC/VID badges stay hidden — a cosmetic list-enrichment, not part of the detail feature. Only pursue if a per-row recording indicator is wanted; costs an N-row recordings lookup or a JOIN in the list query.)

## Lessons (hard-won)
- **`db.batch([stmt,stmt])`** is THE atomic-multi-write pattern (implicit txn, rejects+rolls-back on any failure). Convention ref: `credits.ts`. Test it by giving the D1 mock a capturing `prepare`+`batch` jest.fn (+ `mockRejectedValueOnce` for the atomicity/rollback assertion).
- When a source change alters a function's D1 access pattern (e.g. dbUpdate→batch), grep **ALL** `*.test.ts` calling it + update every mock; verify each **STANDALONE** — co-scheduled `--runInBand` runs mask which suite actually fails (iter-19 regression misdiagnosed as a "flake", caught iter 22).
- **Config-tested ≠ behavior-tested (iter 42 DeepSeek routing).** A green test suite can hide a live bug when it tests the CONFIG (`chooseProviderForTier` returns 'deepseek', the base-URL CONSTANT is 'api.deepseek.com') but never the actual ROUTING (does a call ACTUALLY hit api.deepseek.com?). The DeepSeek tier's config was 100% correct + tested, yet every call went to OpenAI's URL because the dispatch dropped the provider. **When auditing a routing/dispatch/selection service, don't trust "config resolves to X" tests — add a BEHAVIORAL test that mocks the network seam (`gatewayFetch`) and asserts the request went to X.** (Sibling of the [[verify-against-source-of-truth]] render-vs-endpoint blind spot: selection-vs-dispatch.)
- **Re-pathing an FE call must update its Karma SPEC assertions too, not just call sites (iter 38 caught iter 32).** iter 32 re-pathed the integrations DELETE `/integrations/` → `/integration-providers/` (code + 2 call sites) but left `site-detail.component.spec.ts:389` asserting the old path. It stayed RED from iter 32→38 because the FULL frontend Karma suite wasn't run to completion (state claimed "Karma 1592/1592 iter 4" — stale). **Lesson: (a) when touching any FE `/api` path, grep the SPECS for the old string + update assertions; (b) a frontend change's verification MUST run the WHOLE `npm run test:ci` (1610 tests, ~2min) — a scoped run hides stale specs elsewhere. Adding ANY spec = run the full suite (gotcha #9 order-flake + surfaces long-hidden reds).**
- **Angular control-flow migration = official schematic, not hand-edits (iter 37).** `ng g @angular/core:control-flow --path=<dir> --interactive=false` migrates `*ngIf`/`*ngFor`/`*ngSwitch` → `@if`/`@for`/`@switch` AND is SMART: it removes the now-unused `CommonModule` import + swaps in the SPECIFIC pipe (`CurrencyPipe`) a template still needs — the DELETE pass is done for you. `--path` scopes it (avoid the BLOCKED admin-shell). Dry-run first to confirm scope. After: the only residual `*ngIf`/`*ngFor` are STALE PROSE COMMENTS (perl `s/\*ngIf/@if/g` cleans them — templates are already migrated so it only hits comments). Verify via `ng build` (AOT compiles EVERY template even tree-shaken ones → a broken migration or wrongly-dropped CommonModule = loud compile error) since site-kit has 0 Karma specs. ⚠️ `\| number` grep false-positives on TS union types (`x: string \| number`) — read context.
- **Grep-close the CLASS before claiming a sweep (iter 35).** Fixing the instance in front of you under-delivers when the same bug shape lives in sibling files. iter 35 started with 2 instances found by local reading (site-generation inline + cron); the class-grep (`FROM memberships`/`JOIN memberships` minus `deleted_at`, then READ each multi-line context) surfaced **3 MORE** in separate service files (`notify_site_built`, `notify`, `build_limits`) that the initial reading never touched. The tree-wide grep + per-hit context-read is what converts "fixed a bug" into "swept the class." ⚠️ Single-line grep is only a CANDIDATE list — multi-line queries carry `deleted_at IS NULL` on the NEXT line (`weekly_digest`/`auth_org`/`wallet` were false-positive "misses"); always read the surrounding lines before judging.

## ⚠️ SURFACED — Brian decisions (approval-tier, NOT auto-fixed)
- **Wallet credit double-spend** (iter 18): `creditWallet` (`wallet.ts:445`) idempotency TOCTOU — `wallet_transactions.stripe_event_id` is a plain INDEX, **not UNIQUE** (`0036_wallet_billing.sql:80`) → concurrent Stripe webhook replay double-credits. Fix (needs approval + prod migration): `UNIQUE(stripe_event_id) WHERE … NOT NULL` + insert-first/`INSERT OR IGNORE`-guarded credit + read-back post-UPDATE balance for the ledger snapshot.
- **hey@ unlimited?** (iter 11): 1-line in `isUnlimitedOrgOwner` (build_limits.ts) — grants FREE UNLIMITED COMPUTE, a cost decision.
- **site_benchmarks schema** (iter 17, low): lacks `updated_at`+`deleted_at`. Runtime self-healing retry already fixes the data-loss bug; a migration is cosmetic convention-alignment (prod-apply to BOTH D1s).

## Non-failing frontend build warnings (tracked, do NOT re-hunt)
1. `Unexpected "^"` CSS warning ×2 in `build:prod` — a Tailwind v4 / lightningcss compilation artifact (NOT a source literal; exhaustively verified iter 4). Cosmetic, non-failing. Fix would be Tailwind/lightningcss-config level.
2. `social.component.ts` inline styles **32.86 kB > 28 kB** budget. iter-5 proved NOT a safe cold trim (dense CSS, no dead rules; deep authed component). Resolution = god-component SPLIT behind a Browserbase 6bp visual gate (fresh context) — mirror the ag-grid→TanStack perf-wave precedent. Do NOT blind-trim.

## Next target
**iter 43: image-gen Stability fallback aspect-ratio fix** (square→16:9 wrong-shape bug in the DALL-E→Stability fallback; +2 tests; deploy `15da90c6`). The iter-42 lead → CLOSED (real bug found + fixed). **The "unreviewed production behavior / dispatch-blind-spot" vein remains FRUITFUL** (iters 42-43 both found real live bugs there). **Fresh candidates (avoid BLOCKED admin-shell + Sentry/analytics-owned services):** (a) more unreviewed production services — `rag.ts` (Vectorize embed/query — does the embed model + index dispatch match?), `chat_synthesis.ts`, `site_serving.ts` (hot-path content-type/top-bar — the documented `marketingPath` vs `path` gotcha), `google_places.ts` (Places dispatch/parsing), `openai_research.ts` — audit BEHAVIORALLY (does the dispatch/fetch/parse ACTUALLY do what config says?). (b) constructor DI → `inject()` on a safe frontend dir. (c) knip/jscpd dead-code delta. (d) honest checkpoint. ⚠️ Keep preferring BEHAVIORAL audits (mock the network/parse seam, assert the actual output) over config re-reads — per the iter-42 config-vs-behavior lesson. **Found by auditing unreviewed production behavior.** **Fresh candidates (avoid BLOCKED admin-shell + Sentry/analytics-owned services):** (a) still-unreviewed production services — `rag.ts` (Vectorize/embeddings), `image_generation.ts` (DALL-E→Stability fallback — SAME dispatch-blind-spot class as the DeepSeek bug; audit whether the fallback ACTUALLY hits Stability), `chat_synthesis.ts`, `site_serving.ts` (hot-path content-type/top-bar), `google_places.ts`, `openai_research.ts` — audit for BROKEN BEHAVIOR / wrong-dispatch / error-handling (behavioral, not config). (b) constructor DI → `inject()` on a safe frontend dir (mechanical). (c) knip/jscpd dead-code delta. (d) honest checkpoint. ⚠️ **Prefer BEHAVIORAL audits (does the dispatch/fallback ACTUALLY route where config says?) over config re-reads** — per the iter-42 config-vs-behavior lesson. **Prior fruitful lead (iter 40) now CLOSED.** **✅ site-kit axis CLOSED (iters 37-39) — do NOT return without new evidence.** Frontend control-flow DONE (iter 37); signal-input migration DECLINED by schematic + detector-coupled (skip). **Fresh candidates (avoid BLOCKED admin-shell + Sentry/analytics-owned services):** (a) **constructor DI → `inject()`** via `ng g @angular/core:inject-migration --path=<safe non-blocked dir>` — mechanical, no template/detector coupling (48 files repo-wide; only if genuinely cleaner, not diff-for-diff); (b) **genuinely-unreviewed backend service w/ NEW evidence** — production services (higher real-user leverage than Storybook-only site-kit): `rag.ts`/`external_llm.ts`/`image_generation.ts`/`site_serving.ts`/`contact.ts`/`chat_synthesis.ts` (audit for broken behavior / error-handling / business-logic, NOT the already-swept data-access); (c) a fresh knip/jscpd dead-code delta (net deletion); (d) honest checkpoint. Verify frontend via FULL `npm run test:ci` (NOT scoped — hides stale specs) + Storybook/Browserbase render. **⚠️ BACKEND DATA LAYER IS DEEPLY CONVERGED — iter 36 audited 6 surfaces CLEAN** before finding the one cache bug: input-param bounds (all `Math.min`/`Math.max`-guarded across api.ts/analytics/forms/super_admin/apps/agents/copilot — only cosmetic `depth` NaN edge at api.ts:7719, low-value), `task_inbox.ts` (`resolveTask` TOCTOU-safe via `WHERE resolved_at IS NULL` + `changes===0`; concurrent sweeps idempotent), all 3 flag override-write paths (invalidate correctly), `db.ts` (dbQuery/dbExecute LOG+return `error`; dbInsert/dbUpdate timestamp-fallback). **Future iterations should STOP re-hunting backend data-access (soft-delete/atomicity/IDOR/dbInsert/input-bounds/cache — all swept or guarded by 12 detectors) and PIVOT to: (a) frontend Angular/Spartan/a11y/E2E-coverage gaps [⚠️ admin-shell BLOCKED], (b) genuinely-unreviewed UNREVIEWED-tier services with NEW evidence, (c) an honest verification checkpoint.** iter 35 (soft-delete notification-recipients) + iter 34 (detector health) + iters 31-33 (route-integrity) all at rest. **Detector-gate health restored (iter 34); route-integrity axis at a resting point (iters 31-33)** — detector shipped (31), 1 LIVE-bug collision fixed (32 integrations), 2 remaining dups ANALYZED + DEFERRED (33): both need a DELIBERATE one-way-door decision, NEITHER is a live bug (both FE-unconsumed) — do NOT force them in a loop slice. When picked up (deliberate, review-recommended tier): `GET /api/admin/domains` = decide the canonical contract (rich paginated api.ts:5838 vs sites-grouped ai_admin.ts:2329) then remove/re-path the other; `GET /` = focused host-routing slice deleting the moot :976 + its 2 orphaned helper modules (llm_landing_page.ts, platform_services.ts) + the llm_landing_page test. Full analysis in the `check-duplicate-routes.mjs` ALLOWLIST comments. **The loop should PIVOT to a fresh target:**
- **site-mcp tokens CRUD** — only when `site_mcp_server` is promoted (per CLAUDE.md convert-on-promotion); table (`site_mcp_tokens`, 0514) + FE + contract ready. Still DARK → not a live bug.
- **New surface** — `orgs`/`hostnames` soft-delete edge queries, a fresh knip/jscpd delta. An honest verification checkpoint is legitimate if nothing safe+high-value surfaces.
- If neither fits the budget: another new surface — `orgs`/`hostnames` soft-delete edge queries, or a fresh knip/jscpd delta. Honest checkpoint legitimate if nothing safe+high-value.
- **Bigger waves (fresh context + Browserbase visual gate):** social.component split; ag-grid→TanStack.
- **Careful-only:** `build_validators` / `voice_webhooks` jscpd clones (sensitive; extract only with full test verification).
- If nothing safe+high-value surfaces: an honest verification checkpoint is legitimate — do NOT force marginal/risky changes. Future ≥3-instance bug classes → add a `check-*.mjs` detector + fixture + CI step.
