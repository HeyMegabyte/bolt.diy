{
  "tasks": [
    {
      "id": "d029784d",
      "cron": "7,27,47 * * * *",
      "prompt": "Convergence loop fire (projectsites.dev) — WHOLE-APP COMPLETION via GROUND-TRUTH RECONCILIATION (multi-agent, self-populating). Read apps/project-sites/_CONVERGENCE_TASKS.md § P0-ADMIN top (the \"🔁 Pass … GROUND-TRUTH RECONCILIATION\" block is the live system) + apps/project-sites/CONVERGENCE_PROMPT.md, then execute ONE pass:\n\n1. FIND — automatic + comprehensive, as brian@megabyte.space via Browserbase (creds: get-secret BROWSERBASE_API_KEY / BROWSERBASE_PROJECT_ID / E2E_TEST_PASSWORD; test-login inside the browser to seed ps_session {token, identifier}):\n   (a) D1 ground-truth sweep — COUNT brian's real records (org-brian-001 / site-megabytespace-001) per owner-scoped table = the \"must-show-data\" map (wrangler d1 execute project-sites-db-production --remote --command).\n   (b) node apps/project-sites/e2e/admin-verify/reconcile-surfaces.mjs — each DISPLAY endpoint (fetched as brian in-browser) vs its ground-truth count.\n   (c) node apps/project-sites/e2e/admin-verify/scan-admin-hub.mjs — read EVERY rendered counter/KPI on /admin; THEN extend/Browserbase-scan EVERY admin route (/admin + each section) reading all counters/KPIs/tables/charts, capturing console errors + failed requests + crashes.\n   FLAG every: counter/value/list/chart showing 0/empty/\"not available\"/\"no traffic\"/\"not run\" while D1 HAS data (LYING-EMPTY / wrong-source per rule verify-against-source-of-truth); any console error / failed request / crashed section; any broken/ugly UI. The endpoint reconciler CANNOT catch frontend-computed-counter bugs — the rendered-counter scan is mandatory.\n\n2. SELF-POPULATE — APPEND every NEW finding as a \"[ ]\" item to _CONVERGENCE_TASKS.md § P0-ADMIN (dedupe vs existing). The ledger grows itself each fire.\n\n3. FAN OUT + FIX — spawn 4-5 parallel agents in ONE message (distinct files) to fix the top findings; author shared/interlocking code in the main thread (worktree agents auto-clean). Deploy: frontend = `npm run build:prod && npm run deploy:production` (R2, no Docker); worker = `npx wrangler deploy --env production` (Docker + GLOBAL CF key). Re-verify LIVE as brian — 0 console errors, every counter reconciles to ground truth, screenshots to artifacts.\n\n4. Standing directives (Brian): reconcile display-vs-authoritative-store, not just render-integrity; functional completeness > axe strictness (a11y advisory except critical); test every input with all value-domains; commit+push every green slice (git -c commit.gpgsign=false; git pull --rebase --autostash origin main; NEVER commit .claude/scheduled_tasks.json; stage apps/ + docs/ only).\n\n5. Drive the WHOLE app to completely-finished AND fully real-world prod-tested. NEVER stop on \"admin verified\" or a test count; NEVER propose deleting this cron.",
      "createdAt": 1786043031693,
      "recurring": true,
      "createdBySessionId": "c37b196f-7e52-4af9-a2c5-73cd10da4bb3",
      "createdByPid": 66925,
      "createdByProcStart": "Fri Jul 31 01:35:28 2026"
    }
  ]
}
