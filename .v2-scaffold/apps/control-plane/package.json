{
  "name": "@org/control-plane",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.0",
    "@hono/zod-validator": "^0.4.2",
    "zod": "^3.23.8",
    "stripe": "^17.5.0",
    "@simplewebauthn/server": "^11.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "wrangler": "^4.0.0",
    "vitest": "^2.1.8",
    "typescript": "^5.9.0"
  }
}
