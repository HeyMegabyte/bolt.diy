/**
 * `@org/feature-bookings` — role-aware booking list + detail surfaces.
 *
 * Mounted at `/dashboard/bookings/*` via `loadChildren` from
 * `@org/dashboard`'s `dashboardRoutes` table.
 */
export * from './lib/bookings-list/bookings-list.component';
export * from './lib/bookings.routes';
