{
  "name": "psctl",
  "version": "0.1.0",
  "description": "CLI for the Project Sites Public API — psctl auth login, sites list/get/create, deploy, snapshots, logs",
  "type": "module",
  "bin": {
    "psctl": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build src/cli.ts --outdir dist --target node --format esm"
  },
  "dependencies": {
    "@projectsites/sdk": "file:../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
